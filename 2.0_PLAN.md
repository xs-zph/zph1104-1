# AI 客服工单系统 2.0 —— 架构诊断与改造规划

> 基线：`master` 首次提交 `3f144bb` + tag `v1.0-baseline`（2026-08-14，已本地跑通验证）

---

## 一、1.0 体检结论（现状可运行 ✅）

| 检查项 | 结果 |
|--------|------|
| 依赖 | Python 3.11.9 / fastapi 0.140 / uvicorn 0.51 / pymysql 2.2.8 / chromadb 1.5.9 |
| 服务 | `/health` ok，MySQL 3308 可连，DeepSeek 密钥已配置 |
| 鉴权 | admin / user 登录正常，token 会话正常 |
| LLM 链路 | 发「你好」→ DeepSeek 正常回复（2.6s） |
| 指标 | 68 张工单，自动处理率 73.5%，分类准确率 100%，**平均耗时 5.4s/单** |

**结论**：1.0 功能完整、能跑通，但存在「架构债」和「核心链路与需求不符」两个层面的问题，是 2.0 要解决的。

---

## 二、核心问题诊断（1.0 要解决的问题）

### 🔴 问题 1（最重要）：处理链路与需求不符 —— 没有「RAG 优先」

**你 2.0 的期望**：先查 RAG 知识库 → 解决不了再调 DeepSeek 大模型 → 实在不行转人工。

**1.0 实际链路**：`分类(LLM) → 直接进 Agent(LLM + 工具)`，RAG 只是 Agent 里众多工具之一（`search_faq`），**没有「RAG 命中就直接答」的短路**。

具体证据（[app/router.py:109-130](app/router.py#L109-L130)）：所有可自动分类的问题一律走 `agent.run_agent()`，而：
- [app/responder.py:39](app/responder.py#L39) 的 `rag_reply()` —— **定义了但全项目无一处调用（死代码）**
- [app/responder.py:29](app/responder.py#L29) 的 `template_reply()` —— 同样死代码
- [app/rag.py:316](app/rag.py#L316) 的 `retrieve_docs()` / `ingest_documents()` —— 只在 `scripts/demo_rag.py` 演示脚本用，生产流程从没走过

**后果**：每一单，哪怕是最简单的 FAQ 咨询，都要付出「分类 LLM + Agent LLM + 工具多轮」的完整开销——这就是平均耗时 5.4s、且成本高的根源。

### 🟠 问题 2：性能 —— 单均 5.4s
每单至少 1~2 次大模型调用（分类 + Agent），Agent 内部还可能多轮工具调用。RAG 优先能直接把 FAQ 类问题从「秒级」降到「毫秒级」。

### 🟠 问题 3：数据库 —— 无连接池、每次新建连接
[app/db.py:65](app/db.py#L65) 的 `_connect()` 被每个查询函数调用一次，每次都是「新建连接 → 用完关闭」。68 张工单 = 数百次建连开销，高并发下会打满 MySQL 连接数。

### 🟠 问题 4：同步阻塞
全项目同步 `def` + `requests` 阻塞调用（[app/llm.py:74](app/llm.py#L74)）。一个慢请求会占住一个 worker 线程等 120s 超时，无法并发。

### 🟠 问题 5：架构 —— 单文件入口 + 平铺函数
- 所有路由挤在 [app/main.py](app/main.py)（249 行），登录/工单/订单/知识库/微信全混在一起，无 `routers/` 分层。
- `db.py` 平铺 20+ 个函数，无 DAO/Service 分层。

### 🟠 问题 6：安全隐患
| 问题 | 位置 | 说明 |
|------|------|------|
| 工单越权 | [app/main.py:108-120](app/main.py#L108-L120) | `GET /api/tickets` 和 `/api/tickets/{id}` 收了 `username` 却不用，**任何登录客户能查到全部人的工单** |
| 弱密码哈希 | [app/auth.py:14-23](app/auth.py#L14-L23) | `sha256 + 固定盐`，README 自己也标注要换 bcrypt |
| 内存会话 | [app/auth.py:17](app/auth.py#L17) | token 存内存字典，无过期时间、服务重启即失效、单机不可扩展 |
| 无归属兜底查询 | [app/db.py:277](app/db.py#L277) | `get_order_by_id` / `list_all_orders` 不校验归属，供「未登录/微信」场景，属设计隐患 |

### 🟡 问题 7：其他工程债
- [app/llm.py](app/llm.py) 无重试、无流式、超时固定 120s。
- 缓存/记忆/会话全是进程内存态，重启即丢（演示可接受，生产不行）。
- [app/main.py:33](app/main.py#L33) 已写 `version="2.0.0"`，与 README/1.0 不一致。

---

## 三、2.0 核心：RAG → 大模型 → 转人工 三级兜底链路

### 目标流程

```
客户消息
   │
   ▼
① 脱敏 privacy.mask_sensitive()
   │
   ├─ 闲聊/寒暄 ──────────────► LLM chat（轻量，直接回）
   ├─ 时间/天气 ──────────────► Agent 工具（get_time/get_weather）
   │
   ▼
② 【RAG 优先】检索知识库（faq 问答对 + docs 文档块）
   │
   ├─ 命中且相似度 ≥ 阈值 ────► 直接返回知识库答案（不调大模型，毫秒级）
   │
   ▼
③ 未命中 → 分类(LLM) → 条件路由
   ├─ 低置信度 / 纠纷 / 投诉 ──► 转人工
   │
   ▼
④ Agent（DeepSeek + 工具：订单/物流/退款/再查RAG）
   │
   ├─ 工具都拿不到有效结果 ────► 转人工
   │
   ▼
⑤ 落库 + 记忆 + 指标
```

### 关键设计点

1. **相似度阈值 `RAG_HIT_THRESHOLD`**：ChromaDB 默认返回 L2 `distance`（越小越相关），需统一映射成 0~1 分数再设阈值，避免「查不到还硬答」。可仿照现有 `CONFIDENCE_THRESHOLD=0.8` 的做法，用 `scripts/tune_threshold.py` 的思路扫出最优值。
2. **RAG 命中直接返回**：按你的要求，命中就不调 DeepSeek，直接把知识库答案（可带「已参考知识库」标记）返回，这是性能翻盘的关键。
3. **保留 Agent 内的 `search_faq` 作为兜底**：RAG 优先没命中时，Agent 内部仍可再查一次知识库，两级都不放过。
4. **三级可观测**：`reply_source` 增加 `rag` 档，指标面板能看出「多少单靠 RAG 直接解决、多少单靠 LLM、多少单转人工」。

---

## 四、2.0 四方向分阶段改造清单

### MCP 工具接入（已开始）

- [x] 第一阶段只读接入：订单列表、订单状态、物流进度、退款进度
- [x] 本地 `stdio` MCP Server，Agent 通过 Client Adapter 动态发现工具
- [x] 当前登录用户名由服务端注入，不进入模型可控参数；数据库继续强制订单归属过滤
- [x] MCP 发现或调用失败时回退现有内置工具
- [ ] `cancel_order` 等副作用能力：补充人工确认、幂等、权限和审计后再迁移

### P1 架构与工程化（地基，先做）
- [x] **角色职责拆分**：`admin` 仅负责账号/密码/权限，`manager` 只读运营仪表盘，`agent` 仅处理转人工工单，`customer` 仅使用客户侧服务
- [x] **后端权限控制**：客服页面、工单操作和账号级客服权限均由服务端依赖校验，前端只负责导航与交互
- [x] **账号审计**：管理员创建、修改、停用和重置密码均写入账号审计日志，并保护最后一个启用中的管理员账号
- [x] **场景化 Skills 召回**：Skill 元数据注册 + 场景规则评分 + Agent Top-K 工具子集
- [x] **修复越权**：`/api/tickets`、`/api/tickets/{id}` 按 `username` 过滤
- [x] **密码升级**：sha256+固定盐 → bcrypt（旧密码成功登录后迁移）
- [x] **会话 TTL**：进程内会话增加可配置过期时间（持久化会话仍待 Redis/JWT）
- [x] **重构 router**：按第 3 节实现「RAG 优先 → LLM → 转人工」三级链路（核心）
- [x] **MySQL 连接池**：已用项目内有界连接池替换每次新建连接（暂未引入 SQLAlchemy / dbutils）
- [x] **会话持久化**：已接入可选 Redis；未配置 Redis 时保留单机内存降级
- [ ] **分层路由**：`main.py` 拆成 `routers/{auth,tickets,admin,orders,faq,wechat}.py`
- [ ] **异步化（可选）**：`requests` → `httpx`，`def` → `async def`（配合连接池/流式）
- [ ] **LLM 健壮性**：加重试、可配超时、流式输出
- [x] **测试**：已补核心链路、安全、MCP、图片、画像、基础设施和坐席协作回归测试（当前使用 `unittest`，尚未迁移 pytest）
- [ ] **Docker + docker-compose**（MySQL + 应用 + ChromaDB）
- [ ] **Swagger 分组标签**（FastAPI 自带 /docs，补 tags 即可）

### P2 前端体验重构
- [x] 客服工作台实时消息（SSE，连接失败时轮询降级；暂未实现 WebSocket）
- [x] 系统管理员后台（账号权限管理、审计记录）与经理仪表盘（人工处理效果柱状图）
- [ ] 工单看板（待处理/处理中/已办结拖拽）
- [ ] 移动端适配（当前仅有基础响应式样式）
- [ ] 可视化大屏（当前已有后台指标和趋势统计，尚未做独立大屏）

### P3 接入真实业务数据
- [ ] 对接真实订单/物流/退款系统或三方 API，替换 `db.seed_orders()` 的模拟数据
- [x] 保留「订单归属校验」的设计，接入后仍是服务端注入身份

### P4 新增核心功能
- [x] 工单 SLA 时效 + 超时自动升级（后台线程定时扫描，幂等标记 `sla_breached`、提升为 `urgent`，并通过 SSE 通知客服后台）
- [x] 坐席分配 / 转派
- [ ] 工单批量导入（Excel）
- [ ] 知识库多格式导入（PDF/Word，复用 `chunk_text`）
- [ ] 客户意图 / 情绪分析报告（已有单工单情绪识别和趋势统计，尚未形成完整报告）

---

## 五、清理清单（2.0 要删的冗余）

> 原则：删前先跑 `git grep` 确认零引用，删完再全量回归。基线 tag 已留，可随时回退。

| 文件/函数 | 判定 | 处理建议 |
|-----------|------|----------|
| `app/responder.py` 的 `template_reply()` | 死代码（0 调用） | 删 |
| `app/responder.py` 的 `rag_reply()` | 死代码（0 调用） | 逻辑并入 P1 的「RAG 优先」链路后删 |
| `templates/replies.json` | 仅被死函数 `template_reply` 读 | 删 |
| `prompts/respond.py` | 仅被死函数 `rag_reply` 用 | 2.0 重做 RAG 优先时改写或删 |
| `app/rag.py` 文档切块链路 `chunk_text/ingest_documents/retrieve_docs/get_docs_collection` | 生产流程 0 使用，仅 `demo_rag.py` | P4 要做多格式导入则保留；否则归档 |
| `scripts/run_demo.py` `demo_rag.py` `gen_tickets.py` `analyze.py` `review_errors.py` `tune_threshold.py` | 演示/评测一次性脚本 | 归档到 `scripts/legacy/` 或删（`tune_threshold.py` 阈值扫描思路保留，P1 复用） |
| `prompts/analysis.py` `prompts/review.py` | 仅评测脚本用 | 随评测脚本一起归档/删 |

**保留**：`app/responder.py` 的 `chat_reply()`、`scripts/seed_faq.py`（初始化知识库必需）、`data/docs/*.md` 与 `data/faq.md`（知识库源）。

---

## 六、建议实施顺序

1. **P1 核心**：先落地「RAG 优先 → LLM → 转人工」三级链路（问题 1 + 2 一起解决），顺手清理第 5 节的死代码。
2. **P1 工程化**：连接池 + 越权修复 + bcrypt + 分层路由 + 测试。
3. **P2 前端** / **P4 功能** 视答辩时间穿插。
4. 每阶段一个小 tag（`v2.0-p1-rag` 等），随时可回退。

---

## 七、待确认事项

- [x] `RAG_HIT_THRESHOLD` 已落为 `RAG_MAX_DISTANCE=0.6`（按当前距离定义；后续仍需评测集校准）
- [x] RAG 命中后直接返回知识库答案，不再调用大模型
- [ ] 评测/演示脚本（第 5 节 `scripts/legacy`）是归档还是直接删
- [ ] git 身份：当前用占位 `pc <pc@local>`，推 GitHub 前需改成你的真实 name/email
