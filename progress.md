# 项目进度日志

## 2026-09-10

### 本次完成

- 定位“我生气了”显示“系统开小差”的真实原因：
  - 后端 RAG 命中结果包含 `datetime`；
  - `json.dumps()` 序列化失败；
  - 接口返回 500；
  - 前端统一异常文案掩盖了真实原因。
- 增加纯情绪反馈的本地安抚路径。
- 增加 RAG 序列化兜底。
- 优化前端 API 错误提示。
- 修复“气炸”误伤“空气炸锅”的关键词边界。
- 新增情绪与 RAG 序列化回归测试。
- 完成解决日记、发现记录和后续任务规划更新。

### 验证

```text
python -m pytest -q
172 passed, 224 warnings

python -m compileall -q app
通过

node --check static/app.js
通过

git diff --check
通过
```

真实接口回放：

```text
输入：我很生气
HTTP：200
reply_source：chat
status：auto
结果：返回自然安抚，不转人工，不显示系统异常
```

### 当前工作区状态

- 本地服务已启动：`http://127.0.0.1:8000`
- 当前阶段 1：核心客服流程稳定性，已完成并进入持续回归
- 下一阶段：FAQ 多缓存、检索切块、rerank、Recall/Precision 可视化
- 尚未执行 GitHub 推送

### 遇到的问题与处理

| 问题 | 处理 |
|---|---|
| PowerShell 中没有独立 `pytest` 命令 | 改用 `python -m pytest` |
| 旧服务进程仍加载旧代码 | 停止旧 uvicorn 后用 `--reload` 重启 |
| “气炸”误命中“空气炸锅” | 将关键词收紧为“气炸了/气炸我了”，并增加反例测试 |
| 规划文件残留简历任务 | 重写为 AI 客服项目规划，不删除 `2.0_PLAN.md` 历史架构文档 |

## 后续记录格式

每次继续开发时按以下格式追加：

```markdown
## YYYY-MM-DD

### 本次目标


### 完成内容


### 验证结果


### 遗留问题

```

## 2026-09-11：建立任务总结台账

### 本次目标

- 将每次功能开发的目标、改动、验证和遗留问题统一沉淀。
- 让后续迭代、面试复盘和 GitHub 发布不依赖聊天记录。

### 完成内容

- 新增 `任务总结.md`，整理当前已完成任务和项目状态。
- 新增 `任务总结模板.md`，固定后续任务的总结字段。
- 在 `task_plan.md` 增加规划、过程、发现、解决日记和任务总账的职责边界。
- 在 `README.md` 增加项目迭代记录入口。

### 验证结果

- 文档文件均位于项目根目录，便于 GitHub 直接查看。
- 当前任务不涉及运行时代码，沿用最近一次全量回归：`224 passed`。

### 遗留问题

- 后续每个阶段完成时需要同步更新任务总账，不能只更新代码。
- GitHub 发布前需要检查新增中文文档、链接和未提交文件。

## 2026-09-10：阶段 4.8 售后失败尝试与重试

### 本次目标

- 为售后副作用操作增加独立尝试记录。
- 让失败申请可以沿用原申请号重试，避免重复生成业务申请。

### 当前动作

- 已确认用户批准阶段 4.8 设计。
- 先按 TDD 增加数据库尝试记录、失败恢复和重试权限边界测试。
- 之后接入客户侧失败路径、客服端重试接口和经理审计展示。

### 验收重点

- 失败原因不能被 `processing` 或其他状态覆盖。
- 重试不创建新申请，不绕过订单归属和状态校验。

### 本次完成

- 新增 `after_sale_attempts` 表，记录 `started / succeeded / failed`。
- 数据库新增尝试开始、完成、查询和汇总接口。
- 客户侧退款、维修、退货确认和取消订单执行接入尝试记录。
- 客服侧退货、退款、维修、取消订单均接入失败状态和尝试轨迹。
- 新增 `POST /api/escalations/{ticket_id}/after-sale-retry`。
- 重试只读取原申请号和原幂等键，不调用创建申请接口。
- 重试前重新校验工单状态、客服归属、售后类型、客户订单归属和订单当前状态。
- 客服工作台显示失败原因和“重新提交”按钮。
- 客服、经理审计弹窗展示每次尝试的时间、状态、操作人和失败原因。
- 客户侧公开数据隐藏坐席操作人，仅保留业务化失败状态和原因。
- 修正最近一次成功尝试不应显示历史失败原因的统计边界。

### 验证结果

```text
售后专项测试：51 passed
全量测试：224 passed
Python compileall：通过
JavaScript node --check：通过
git diff --check：通过
```

### 遗留问题

- 生产多实例部署时需要把数据库迁移纳入一次性发布迁移流程，避免多个实例同时执行初始化。

## 2026-09-10：阶段 2 检索与缓存审计

### 当前发现

- FAQ 多层缓存、语义缓存、rerank、离线 RAG 评测和经理端基础展示已经存在。
- 当前 `cache_stats()` 虽然有命中次数，但缺少统一的请求数、命中数、未命中数和缓存层分布口径。
- 评测结果已经能计算 Recall@5、Precision@3 和边界保护率，但还需要把缓存命中、直接 FAQ 回答和向量检索命中区分开。
- 当前阶段不先盲目调整切块大小或阈值，先补指标和测试，再根据真实评测结果调参。

### 本次工作目标

- 统一 FAQ 缓存统计字段和计算方式。
- 给 FAQ 结果补充明确的命中层：memory、redis、semantic、vector。
- 让离线评测输出缓存污染保护和可解释明细。
- 保持现有接口兼容，经理仪表盘继续使用旧字段，同时增加新字段。

### 本次完成

- FAQ 结果明确标记最终来源层：`memory / redis / semantic / vector`。
- 缓存统计改为按请求计数，新增：
  - 请求数
  - 缓存命中数
  - 向量命中数
  - 直接回答数
  - 未回答数
  - LLM 节省数
- 离线 RAG 评测不再污染线上缓存统计。
- 使用 `ContextVar` 处理并发下的统计隔离。
- 经理仪表盘新增直接回答率和缓存层命中分解。
- 新增缓存统计去重、向量来源标记、评测隔离回归测试。

### 验证结果

```text
python -m pytest -q tests/test_rag_pipeline.py tests/test_rag_eval.py
12 passed

python -m compileall -q app
通过

node --check static/manager.js
通过

git diff --check
通过
```

## 2026-09-10：人工客服售后标注设计审计

### 本次目标

- 继续完善人工客服处理退货问题时的结构化标注能力。

### 已完成

- 检查工单、售后申请、权限、审计日志和客服工作台现有接口。
- 确认工单会话与售后申请是两个不同领域对象，不能混为一个状态。
- 确认当前缺口是客服工作台没有售后标注和补充信息面板，而不是缺少普通聊天能力。

### 下一步

- 先确认标注交互设计和公开测试接口。
- 用户确认后按 TDD 增加行为测试，再实现后端字段/API、审计和客服工作台界面。

## 2026-09-10：人工客服结构化售后标注落地

### 本次目标

- 让人工客服在聊天之外看到并维护“用户需要退货”等结构化售后上下文。

### 完成内容

- 新增 `ticket_after_sale_annotations` 当前标注表，一张工单一条最新标注。
- 保留 `ticket_logs` 作为标注修改历史，记录标注类型、处理阶段和操作人。
- 新增 `AfterSaleAnnotationRequest` 请求模型。
- 新增 `PUT /api/escalations/{ticket_id}/after-sale-annotation`。
- 标注类型支持：取消订单、退货申请、退款申请、换货申请、售后维修。
- 处理阶段支持：待补充信息、待核对订单、待用户确认、已提交售后、处理中、已完成、已关闭。
- 保存订单号时校验订单属于该工单客户。
- 标注保存后发布 `ticket_updated`，客服端实时刷新。
- 客服工作台卡片增加标注区，可填写订单号、商品、原因、商品状态和备注。
- 增加页面资源版本号，避免旧缓存遮住新功能。
- 明确标注不等于提交退货，未调用 `create_after_sale_request`。

### 验证结果

```text
python -m pytest -q
182 passed, 224 warnings

python -m compileall -q app
通过

node --check static/staff.js
node --check static/app.js
node --check static/manager.js
node --check static/profile.js
通过

git diff --check
通过
```

### 使用方式

1. 以客服账号进入 `/staff`。
2. 在工单卡片的“售后标注”区域选择“退货申请”。
3. 选择“待补充信息”或“待核对订单”。
4. 填写订单号、商品、退货原因、商品状态和客服备注。
5. 点击“保存标注”。

此操作只给客服建立结构化上下文，不会自动向客户提交退货申请；真正的退货申请仍需继续核对订单并走确认流程。

### 运行态备注

- 代码测试、编译和前端语法检查均通过。
- 当前 `127.0.0.1:8000` 是已有的 `uvicorn --reload` 进程，监听仍在，但本轮探活请求未及时返回。
- 未强制终止该进程，避免影响用户当前打开的会话；刷新服务后会加载 `staff.html` 中新的资源版本 `20260910-after-sale-annotation`。

## 2026-09-10：人工客服订单核验卡落地

### 本次目标

- 让客服输入订单号后直接看到真实商品和订单状态，解决“只看聊天、不知道商品”的问题。

### 完成内容

- `db.list_escalations()` 为带售后订单号的工单加载当前客户订单。
- 新增 `after_sale.public_order_context()`，只暴露客服核验所需字段。
- 工单列表接口返回业务化订单状态和处理提示。
- 客服工作台增加只读“订单核验”卡。
- 订单核验卡展示商品、订单状态、物流、运单号和退款记录。
- 订单核验卡不提供修改订单入口，不触发退货、退款或取消操作。
- 静态资源版本更新为 `20260910-order-context`。

### 验证结果

```text
订单核验专项测试：26 passed
```

### 使用方式

1. 客服进入 `/staff`，打开一张人工工单。
2. 在“售后标注”里填写客户订单号。
3. 点击“保存标注”。
4. 页面下方出现“订单核验”，显示商品、真实订单状态、物流和退款提醒。

## 2026-09-10：订单号自动匹配商品落地

### 本次目标

- 修正商品仍可手填的问题，让订单号成为商品匹配的唯一依据。

### 完成内容

- 新增 `GET /api/escalations/{ticket_id}/order-context` 订单核验接口。
- 核验接口按当前工单客户查询订单，避免跨客户读取。
- 售后标注保存时重新查询订单，强制写入订单表商品。
- 前端增加“核验订单”按钮。
- 订单号输入框变更/失焦时自动核验。
- 商品字段改为只读，核验成功后自动填充。
- 订单号变化时清空旧商品并重置核验状态。
- 页面缓存版本更新为 `20260910-order-match`。

### 验证结果

```text
订单匹配专项测试：28 passed
前端 JavaScript 语法检查：通过
Python 编译检查：通过
git diff --check：通过
```

### 下一步

- 扩充 `data/rag_eval.jsonl` 的同义问法、错别字、拒答和跨场景样本。
- 记录当前 Recall@5 / Precision@3 基线。
- 再逐项调整切块、召回和拒答阈值。

## 2026-09-10：阶段 2 首轮质量基线

### 完成内容

- 为离线评测增加 `Precision@1`，区分最终答案命中率和 Top3 候选集合纯度。
- fallback rerank 增加中文问题标题的二字/三字短语和业务词重合评分。
- 扩展 FAQ 评测标签，允许同一业务流程的多个正确答案参与评测。
- 保留 CrossEncoder 可用时的原有排序路径。

### 实际结果

```text
评测样本：8
FAQ 样本：7
实时数据保护样本：1
Recall@5：100%
Precision@1：100%
Precision@3：42.86%
FAQ 命中率：100%
边界保护率：100%
```

### 解释

- Precision@1 达到 100%，说明当前测试集里的最终首答案全部正确。
- Precision@3 为 42.86%，说明 Top3 候选仍包含相邻知识，不能直接把 Top3 全部展示给用户。
- 当前 CrossEncoder 模型文件不完整，fallback 仍在工作；下一轮要补模型或继续优化可解释的本地排序。

### 验证

```text
python -m pytest -q
175 passed, 224 warnings

python -m compileall -q app
通过

node --check static/app.js
node --check static/manager.js
通过
```

## 2026-09-10：阶段 2 评测集扩充与切块审计

### 本次目标

- 用真实口语、错别字和跨业务问题扩大 RAG 评测集。
- 记录 FAQ Top1 错误和边界保护失败的具体原因。
- 审计默认文档切块参数，不凭感觉调整大小。

### 完成内容

- `data/rag_eval.jsonl` 从 8 条扩充到 23 条。
- 新增退款、退货、维修、发票、地址等口语/错别字样本。
- 新增物流、退款进度、工单、订单状态、闲聊元问题和天气问题边界样本。
- `app/rag_eval.py` 增加失败原因、Top1 距离、边界原因和汇总计数。
- 统一问题归一化，覆盖「能退呀」「条见」「保不」「咋报修」「地址填错」等表达。
- CrossEncoder 加入进程级不可用记忆，缺模型时不重复加载。
- `metrics.rag_quality()` 返回失败原因汇总，可供经理端展示。
- 将新增真实问法同步到 `data/faq_variants.json`，使服务启动预热时直接命中多层缓存。
- 审计 `RAG_CHUNK_SIZE=420`、`RAG_CHUNK_OVERLAP=60`：
  - 当前文档 420/60 下分别为 2 块，组合为 4 块；
  - 280/40 为 6 块，500/80 为 3 块；
  - 保留 420/60。

### 验证结果

```text
专项 RAG 测试：15 passed
评测样本：23
FAQ 样本：16
边界样本：7
Recall@5：100%
Precision@1：100%
Precision@3：39.58%
边界保护率：100%
```

### 遗留问题

- Precision@3 仍偏低，说明 Top3 候选包含相邻 FAQ；当前不影响 Top1 直接回答，但需要真实 CrossEncoder 模型或候选业务域约束继续优化。
- Qdrant 当前不可用，运行时自动降级 Chroma；CrossEncoder 模型文件不完整，当前使用本地 fallback rerank。

## 2026-09-10：客服提交真实退货申请闭环

### 本次目标

- 让客服在人工工单中真正提交退货申请，避免只修改标注阶段却没有业务申请记录。

### 完成内容

- 新增 `POST /api/escalations/{ticket_id}/return-request`。
- 新增 `ReturnRequestSubmit`，要求客服明确确认后才执行有副作用的提交。
- 后端读取工单当前售后标注，不信任前端传入的商品和客户信息。
- 按“工单客户 + 订单号”重新核验订单归属。
- 订单状态规则：待发货先取消；已发货/运输中先签收；已签收/已完成允许退货；已退货禁止重复申请。
- `after_sale_requests` 增加可空 `ticket_id`，记录申请来源工单并支持老库迁移。
- 提交时使用用户、业务类型、订单号和原因生成幂等键。
- 已有草稿或待确认申请重试时推进为处理中，不重复生成申请号。
- 只有真实申请创建/复用成功后，标注才更新为“已提交售后”。
- 标注保存接口禁止手动写入 `submitted` 阶段。
- 写入 `after_sale_submitted` 审计日志，并发布客服端与客户侧事件。
- 客服工作台增加二次确认、提交中按钮、申请号和处理中状态。
- 客户聊天实时显示申请号，并提示到“我的售后”查看进度。

### 验证结果

```text
退货提交专项测试：24 passed
python -m pytest -q：201 passed
python -m compileall -q app：通过
node --check static/staff.js：通过
node --check static/app.js：通过
git diff --check：通过
```

### 关键业务边界

- “客户想退货”不等于“已经提交退货”。
- “客服标注退货”不等于“已经提交退货”。
- 只有 `after_sale_requests` 中存在真实申请，且状态推进为 `processing` 后，客服工作台才展示“已提交售后”。
- 数据库创建失败或标注更新失败时，不会静默显示成功。

## 2026-09-10：退款与维修客服提交闭环

### 本次目标

- 复用退货提交协议，让客服可以从人工工单真实提交退款和维修申请。

### 完成内容

- 新增 `POST /api/escalations/{ticket_id}/refund-request`。
- 新增 `POST /api/escalations/{ticket_id}/repair-request`。
- 新增 `AfterSaleRequestSubmit` 确认请求模型。
- 抽取退款/维修共用提交内核：
  - 工单状态和客服归属校验；
  - 当前标注类型校验；
  - 订单号、原因和客户归属校验；
  - 订单取消状态拦截；
  - 退款已有退款记录拦截；
  - 幂等键复用已有申请；
  - 申请状态推进为 `processing`；
  - 标注推进为 `submitted`；
  - 审计日志和双端实时事件。
- 客服工作台根据售后类型动态显示“提交退款申请/提交维修申请/提交退货申请”。
- 客服申请号展示改为使用真实申请类型，不再固定显示退货。
- 静态资源版本更新为 `20260910-after-sale-submit`。

### 验证结果

```text
退款/维修/退货专项测试：28 passed
```

### 下一步

- 继续实现取消订单客服接口；它会直接修改订单状态，必须单独验证原子性、幂等性和失败处理。

## 2026-09-10：客服取消订单真实闭环

### 本次目标

- 让客服在工作台处理“订单还未发货，帮我取消”时，真正执行订单取消，而不是只保存一个取消标注。

### 完成内容

- 新增 `POST /api/escalations/{ticket_id}/cancel-request`。
- 后端重新校验工单状态、客服归属、售后类型、客户归属和订单当前状态。
- 已发货、运输中、派送中订单不允许直接取消，返回联系快递拦截、拒收或签收后退货退款的业务指引。
- 新增 `db.cancel_order_with_request()`：
  - `SELECT ... FOR UPDATE` 锁定订单；
  - 订单状态从“待发货”原子更新为“已取消”；
  - 同一事务创建或复用 `cancel` 类型售后申请；
  - 申请状态直接为 `completed`；
  - 任一步失败整体回滚。
- 取消申请使用稳定幂等键，重复点击返回原申请号。
- 取消成功后工单标注才进入“已完成”，普通标注接口不能伪造该状态。
- 客服工作台根据类型显示“提交取消订单”，完成后显示“取消订单已完成”。
- 工单列表支持加载“已完成”售后申请。

### 验证结果

```text
取消订单专项测试：33 passed
全量测试：210 passed
Python 编译检查：通过
node --check static/staff.js：通过
node --check static/app.js：通过
git diff --check：通过
```

### 业务规则

- 待发货：客服确认后直接取消。
- 已发货/运输中/派送中：不能直接取消，建议快递拦截；失败后拒收或签收后申请退货退款。
- 已取消：幂等返回原申请，不重复创建。
- 已完成、已签收、已退货：不允许作为取消订单处理。

## 2026-09-10：售后申请筛选与审计查询

### 本次目标

- 让客服和经理可以按售后业务条件查找真实申请，并查看“谁在什么时候做了什么”，避免只看到一个申请号和模糊状态。

### 完成内容

- 新增客服队列接口：`GET /api/after-sales/queue`。
- 新增客服单工单审计接口：`GET /api/escalations/{ticket_id}/after-sale-audit`。
- 新增经理队列接口：`GET /api/manager/after-sales`。
- 新增经理单申请审计接口：`GET /api/manager/after-sales/{request_no}/audit`。
- 数据层增加售后申请筛选查询，关联来源工单的文本摘要、工单状态和处理人。
- 支持按售后类型、申请状态和订单号筛选，非法筛选值直接返回业务错误。
- 客服和经理接口只返回业务摘要，不返回客户用户名和数据库内部 id。
- 审计日志动作统一转换为业务语言：
  - 更新售后标注；
  - 提交售后申请；
  - 完成取消订单；
  - 工单状态变更；
  - 人工回复。
- 客服工作台增加售后类型/状态筛选，并可在申请卡片打开审计时间线。
- 经理仪表盘增加售后申请追踪区和审计弹窗。

### 验证结果

```text
售后专项测试：44 passed
全量测试：217 passed
Python 编译检查：通过
node --check static/staff.js：通过
node --check static/manager.js：通过
git diff --check：通过
```

### 设计边界

- 客户仍然只能通过 `/api/after-sales` 查询自己的申请。
- 客服只能从人工工作台权限进入申请队列。
- 经理可以查看全量申请，但查询仍然是只读，不提供修改业务状态的入口。
