# AI 客服工单系统 2.0 任务规划

> 目标：把当前项目继续完善为可演示、可测试、可解释、可上线的企业级 AI 客服系统。
> 更新日期：2026-09-10

## 当前状态

- 当前版本：AI 客服小鹏
- 本地服务：`http://127.0.0.1:8000`
- 最近一次全量测试：`223 passed`
- 当前阶段：阶段 4.8 已完成，进入回归维护
- 发布原则：每个阶段都要有回归测试、真实接口回放和解决日记记录

## 项目文档职责

- `task_plan.md`：记录未来阶段、任务拆解和验收标准。
- `progress.md`：记录本次会话实际执行的步骤、命令和测试结果。
- `findings.md`：记录可复用的技术事实、根因和设计决策。
- `解决日记.md`：记录用户问题、排查过程、解决方法和面试讲法。
- `任务总结.md`：记录每个已完成任务的简明结果，作为迭代和面试回顾入口。
- `任务总结模板.md`：新任务完成后复制使用，避免遗漏验证和遗留问题。

更新规则：每个阶段完成后，同时更新 `任务总结.md`、`progress.md` 和 `解决日记.md`；如果产生新的技术结论，再更新 `findings.md`。

## 阶段 0：基线与工程纪律

状态：已完成

- [x] 梳理客户端、客服端、经理端、管理员端主要流程
- [x] 建立路由评测集和核心回归测试
- [x] 保留 `解决日记.md`、`findings.md`、`progress.md` 三份持续文档
- [x] 统一记录路由来源、工单状态、情绪和 RAG 命中信息
- [x] 服务异常不直接丢失用户请求，不默认创建重复人工工单

验收标准：

- `python -m pytest -q` 全部通过
- 关键用户话术可以通过测试集复现
- 每个已修复线上问题都有根因、改动和验证记录

## 阶段 1：核心客服流程稳定性

状态：已完成，持续回归

- [x] 订单、物流、退款进度、工单查询走确定性工具链
- [x] 退款申请先问原因，再收集订单号，最后确认提交
- [x] 退货、维修、取消订单增加确认和幂等处理
- [x] 强烈退货诉求可以进入退货流程，不因普通申请直接转人工
- [x] 人工会话中识别新的 AI 自助诉求，并同步结束旧人工状态
- [x] 纯服务反馈和情绪表达先安抚，不调用 LLM、不转人工
- [x] 修复 RAG 命中结果的时间字段序列化异常
- [x] 前端区分业务错误、服务降级和网络错误

持续回归话术：

- 「我生气了」
- 「我的快递到哪了」
- 「我想退款」
- 「我强烈要求退货」
- 「订单还没发货，帮我取消」
- 「商家拒绝退款，我要投诉」

验收标准：

- 正常查询不误转人工
- 普通退款/退货/维修申请不跳过确认
- 明确投诉和纠纷才转人工
- 情绪表达得到自然安抚，不出现“系统开小差”

## 阶段 2：FAQ 多缓存与检索质量

状态：进行中（第一小步已完成）

目标：减少 LLM 消耗，同时提升 FAQ 召回率和精确率。

- [x] 完成 FAQ 多层缓存：进程内 LRU → Redis → 语义缓存 → 向量检索
- [x] 给缓存命中记录统一 `cache_layer`、命中次数、节省 LLM 次数
- [x] 增加 FAQ 多问法资产，覆盖口语、同义句和错别字
- [ ] 统一 FAQ 缓存失效策略：新增、编辑、停用、恢复后立即失效
- [x] 评估检索切块大小，默认控制在适合问答的中等粒度
- [ ] 文档切块保留标题、业务场景和相邻上下文
- [x] 召回 Top-K 后进行 rerank，再按阈值决定是否直接回答
- [x] 建立 RAG 评测集，计算 Recall@K、Precision@K、命中率和拒答率
- [x] 经理仪表盘展示召回率、精确率、RAG 命中率、缓存命中率和 LLM 节省量

已完成的第一小步：

- [x] 统计按请求去重，区分 `memory / redis / semantic / vector`
- [x] 增加缓存请求数、缓存命中数、向量命中数、直接回答数和 LLM 节省数
- [x] 离线评测暂停线上缓存统计，且使用上下文隔离避免并发串数据
- [x] 经理端显示缓存层分解和直接回答率
- [x] 增加缓存统计与评测隔离回归测试
- [x] CrossEncoder 不可用时使用中文词面 + 向量距离 fallback rerank
- [x] 评测集增加可接受的同义/相邻 FAQ 答案，避免指标虚低

建议验收指标：

- FAQ 评测集 Recall@5 >= 90%
- 高置信 FAQ Precision@1 >= 90%
- 语义缓存命中后不调用 LLM
- RAG 无关问题不强行回答
- 新增或修改 FAQ 后无需重启即可生效

## 阶段 3：Skills 与 MCP 业务闭环

状态：已完成，进入回归维护

- [x] 订单、物流、退款查询接入 MCP 只读工具
- [x] MCP 失败时回退内置工具
- [x] 退款、退货、维修、取消订单已有场景 Skill
- [ ] 为每个 Skill 补充统一输入、输出、失败和审计协议
- [ ] 为取消订单、退款申请、退货申请增加副作用工具的权限检查
- [ ] 所有副作用操作统一做到确认、幂等、审计和可重试
- [ ] 增加 Skill 级测试集：正常、缺参数、错订单、重复提交、跨用户订单
- [ ] 对 MCP 不可用、订单不存在、数据库超时分别提供业务化提示

验收标准：

- 用户能看懂下一步需要提供什么
- 订单号错误不会创建工单
- 重复点击不会重复退款、退货、维修或取消
- MCP 故障不会直接显示技术异常

## 阶段 4：客户侧与管理侧体验

状态：部分完成

- [x] 客户聊天框、侧边栏和小鹏品牌基础体验
- [x] 客服工作台实时消息、接单、转派、回复和结束
- [x] 经理仪表盘已有基础统计和趋势展示
- [ ] 优化客户侧侧边栏在桌面端和移动端的稳定显示
- [ ] 优化输入框层次、快捷提问和多轮流程按钮
- [ ] 统一工单列表为客户可读的业务语言，避免展示内部状态名
- [ ] 完善经理仪表盘：总览圆盘、趋势折线、召回/精确率和缓存指标
- [ ] 对 3D/动效保持克制，优先保证数据准确、可读和加载稳定
- [ ] 使用浏览器回放验证桌面端、窄屏端和刷新后的缓存版本

验收标准：

- 客户能看懂工单状态和下一步动作
- 侧边栏在刷新、登录、移动端切换后仍存在
- 图表数据与接口返回一致
- 动效不遮挡聊天内容，不影响输入和提交

## 阶段 5：企业级可观测性与安全

状态：部分完成

- [x] 角色权限、账号审计、客服工单权限
- [x] 工单 SLA、超时扫描和状态日志
- [x] 用户订单归属校验
- [ ] 增加统一 request_id 到前端错误提示和后台日志的关联
- [ ] 增加错误率、平均响应时间、LLM 超时率和工具失败率监控
- [ ] 增加敏感操作审计查询和导出
- [ ] 完成 Redis、多进程部署和服务重启后的会话验证
- [ ] 复查生产配置、默认密码、日志脱敏和 CORS/CSRF 边界
- [ ] 补充 Docker Compose 启动与健康检查文档

验收标准：

- 发生错误时能通过 request_id 定位完整链路
- 客户只能读取自己的订单、工单和售后申请
- 敏感字段不进入日志、LLM 请求和前端展示
- 服务重启或多进程运行时核心能力仍可用

## 阶段 6：发布与 GitHub 更新

状态：待开始

- [ ] 清理与本项目无关的规划文档和临时文件
- [ ] 检查未提交修改，区分本轮改动和用户已有改动
- [ ] 更新 README：架构、演示账号、启动方式、测试方式和已知限制
- [ ] 运行发布检查、全量测试、编译检查和前端语法检查
- [ ] 检查 `.env`、密钥、数据库账号和日志是否被提交
- [ ] 确认 Git 用户身份和目标远程仓库
- [ ] 按阶段提交清晰 commit，再推送 GitHub
- [ ] 推送后回读远程分支和最新 commit，记录发布结果

发布门槛：

```text
python -m pytest -q
python -m compileall -q app
node --check static/app.js
python scripts/check_release.py
git diff --check
```

## 当前下一步

1. 补齐统一 Skill/MCP 输入、输出、失败码、权限、幂等和审计协议。
2. 增加 MCP/Skills 故障降级和副作用边界测试。
3. 复查客户侧、客服侧和经理侧的失败状态文案与重试入口。
4. 浏览器回放三端关键流程，再进行 GitHub 发布检查和推送。

## 新增阶段 4.8：售后失败尝试记录与原申请号重试

状态：已完成，进入回归维护

- [x] 增加 `after_sale_attempts` 尝试记录表和老库迁移
- [x] 每次退款、退货、维修和取消执行记录开始、成功或失败
- [x] 失败记录保存错误原因、操作人、时间和尝试次数
- [x] 失败申请只允许复用原申请号和原幂等键重试
- [x] 重试前重新校验工单权限、客户归属、订单状态和售后类型
- [x] 客服工作台增加“重新提交”入口
- [x] 经理审计显示失败原因、尝试次数和最近操作人
- [x] 增加失败恢复、重复重试、跨客服和订单状态变化测试

验收标准：

- 失败不会被后续状态更新覆盖，能看到完整失败轨迹。
- 重试不会新建申请号，也不会重复取消订单或重复退款。
- 重试成功后才恢复为处理中/已完成并更新工单标注。

验收结果：

- 售后专项测试 `51 passed`。
- 全量测试 `224 passed`。
- Python 编译、JavaScript 语法和 `git diff --check` 均通过。

## 新增阶段 4.1：人工客服售后结构化标注

状态：已完成

- [x] 确认“意图标注”和“提交售后申请”的领域边界
- [x] 确认客服可编辑的字段、状态和权限
- [x] 确认后端公开测试 seam
- [x] 增加工单售后标注数据与审计记录
- [x] 增加标注读取、创建、更新接口
- [x] 在客服工作台展示退货/退款/换货/维修标注和待补充信息
- [x] 增加结构化标注行为测试和权限测试
- [x] 更新解决日记并运行全量回归

验收结果：

- 客服工单列表返回当前售后标注。
- 客服可在工单卡片中保存退货、退款、换货、维修和取消订单标注。
- 标注包含处理阶段、订单号、商品、原因、商品状态和客服备注。
- 标注写入工单日志并发布 `ticket_updated` 事件。
- 保存标注不会创建 `after_sale_requests`，真正提交售后仍走既有确认流程。
- 订单号填写后会校验当前客户归属。
- 全量测试 `182 passed`。

## 新增阶段 4.2：人工客服订单核验卡

状态：已完成

- [x] 确认订单事实与客服标注的领域边界
- [x] 为带订单号的标注加载当前客户订单
- [x] 返回商品、订单状态、物流、运单号和退款记录
- [x] 对订单状态提供客服下一步处理提示
- [x] 跨客户订单不返回核验信息
- [x] 客服工作台展示只读订单核验卡
- [x] 增加订单核验行为测试
- [x] 更新静态资源版本和解决日记
- [x] 运行全量回归

验收结果：

- 带订单号的售后标注会自动展示真实商品和订单状态。
- 未填写订单号时不会查询订单，也不会展示空的核验卡。
- 订单核验只按“当前工单客户 + 订单号”匹配。
- 已发货、运输中、已签收、已退货和已有退款记录会给出业务化提醒。
- 订单核验卡只读，不会修改订单或提交售后申请。
- 订单核验专项测试 `26 passed`。

## 新增阶段 4.3：订单号自动匹配商品

状态：已完成

- [x] 增加客服订单实时核验接口
- [x] 订单号按当前工单客户归属匹配
- [x] 商品名称强制使用订单表结果
- [x] 前端输入订单号后支持自动核验和按钮核验
- [x] 核验成功后商品字段只读
- [x] 订单号变化时清除旧商品和旧核验状态
- [x] 保存标注时后端再次核验，防止绕过前端
- [x] 增加跨客户订单和商品来源测试
- [x] 更新静态资源版本和解决日记

验收结果：

- 客服输入订单号后，商品从订单表自动匹配。
- 前端不能把手动填写的商品名作为可信数据提交。
- 后端保存时忽略伪造商品名，重新读取订单商品。
- 订单不存在或不属于当前客户时不会保存标注。
- 订单号修改后必须重新核验。

## 新增阶段 4.4：客服提交真实退货申请

状态：已完成，进入回归维护

- [x] 增加客服提交真实退货申请接口
- [x] 服务端重新校验工单、客服归属、客户归属和订单状态
- [x] 退货原因、订单号和确认状态缺失时拒绝提交
- [x] 未发货订单提示先取消，运输中订单提示签收后再走退货
- [x] 已签收/已完成订单创建真实 `after_sale_requests`
- [x] 售后申请关联来源工单 `ticket_id`
- [x] 重复点击使用幂等键，不重复生成申请号
- [x] 已有售后草稿重试时推进为处理中
- [x] 真实申请成功后才允许标注进入“已提交售后”
- [x] 保存标注接口禁止客服手动伪造“已提交售后”
- [x] 写入 `after_sale_submitted` 审计日志
- [x] 通过 `ticket_updated` 和 `after_sale_updated` 通知客服端与客户侧
- [x] 客服工作台增加二次确认、提交中和申请号展示
- [x] 增加数据库迁移、接口行为和幂等回归测试

验收结果：

- 客服填写并核验订单、补充退货原因后，才显示“提交退货申请”。
- 未发货、运输中、已退货订单不会创建退货申请。
- 跨客户订单、无原因、无订单号和非退货标注不会提交。
- 重复提交返回原申请号，不创建重复售后记录。
- 客户侧会收到申请号和“处理中”状态，客服侧工单卡也能看到申请号。
- 退货提交专项测试 `24 passed`，全量测试 `201 passed`。

## 当前下一步

1. 为取消订单增加客服真实执行接口，严格处理订单状态变更和失败回滚。
2. 增加售后申请的权限审计查询和失败重试记录。
3. 在客服工作台补充申请状态筛选，减少人工查找。
4. 阶段 4.5 稳定后再进行 GitHub 发布检查和推送。

## 新增阶段 4.5：退款与维修客服提交

状态：已完成，进入回归维护

- [x] 增加客服退款申请提交接口
- [x] 增加客服维修申请提交接口
- [x] 两类申请复用统一确认、归属校验、幂等、审计和事件通知协议
- [x] 退款已有退款记录时拒绝重复申请
- [x] 取消订单和不匹配的售后类型不会误调用退款/维修接口
- [x] 客服按钮根据售后类型动态展示正确的业务文案和接口
- [x] 更新静态资源版本
- [x] 增加退款、维修接口行为测试

验收结果：

- 退款标注显示“提交退款申请”，维修标注显示“提交维修申请”。
- 两类真实申请都关联当前工单 `ticket_id`。
- 申请成功后标注统一进入“已提交售后”，客户可以收到申请号和处理中状态。
- 全量测试以本轮最终回归结果为准。

## 新增阶段 4.6：客服取消订单真实闭环

状态：已完成，进入回归维护

- [x] 增加 `POST /api/escalations/{ticket_id}/cancel-request`
- [x] 仅允许当前客户的“待发货”订单在线取消
- [x] 已发货/运输中订单返回快递拦截、拒收或退货退款指引
- [x] 使用订单行锁，订单更新与取消申请写入同一事务
- [x] 重复点击复用原取消申请，不重复生成申请号
- [x] 只有订单变成“已取消”后，申请才写入“已完成”
- [x] 取消工单标注由真实接口推进为“已完成”，普通标注不能伪造
- [x] 客服工作台增加取消订单动态提交按钮和完成状态显示
- [x] 客服工单列表加载“已完成”取消申请
- [x] 增加未确认、已发货、成功取消、重复提交和数据库事务测试

验收结果：

- 取消订单不再停留在“只标注、不执行”的假闭环。
- 订单、售后申请、客服标注三者的成功状态一致。
- 专项测试 `33 passed`，全量测试 `210 passed`。

## 新增阶段 4.7：售后申请筛选与审计查询

状态：已完成，进入回归维护

- [x] 增加客服售后申请队列查询接口
- [x] 增加经理全量售后申请查询接口
- [x] 支持按售后类型、申请状态和订单号筛选
- [x] 查询结果附带来源工单摘要，但不返回客户用户名
- [x] 客服可查看单笔申请关联工单的处理记录
- [x] 经理可查看单笔申请的完整来源工单审计
- [x] 将内部审计动作转换为“提交售后申请/完成取消订单”等业务语言
- [x] 客服工作台增加售后类型和申请状态筛选
- [x] 经理仪表盘增加售后申请追踪和审计弹窗
- [x] 增加权限、筛选、脱敏和审计接口测试

验收结果：

- 客服、经理、客户三类查询权限边界清晰。
- 客服和经理看到的是申请状态与处理轨迹，不需要阅读数据库内部状态名。
- 专项测试 `44 passed`，全量测试 `217 passed`。
