# 解决日记（AI 客服工单系统 2.0）

> 记录近期排查出的问题、根因与解决方法，供后续复盘与回归对照。
> 每次解决一批问题，就在这里追加一条。

---

## 2026-08-14：四项体验问题

### 问题 1：产品没有介绍

- **现象**：客户问「你们是做什么的 / 介绍一下产品」，AI 答不上来，只会套「电商平台」的空话，没有任何店铺和商品信息。
- **根因**：
  1. `data/faq.md` 全是退货/保修/发票等**政策**，没有一条「主营品类 / 产品介绍」；
  2. `prompts/agent.py`、`prompts/chat.py` 里系统提示词只写「电商平台」，没有店铺画像。
- **解决方法**：
  1. `data/faq.md` 新增 2 条产品介绍（主营四大品类：智能数码 / 厨房小家电 / 运动户外 / 生活家居）；
  2. `prompts/agent.py`、`prompts/chat.py` 增加「店铺与产品介绍」块，让 LLM 任何时候都认识自家产品；
  3. 品类从 `db.seed_orders()` 的真实演示商品（耳机/空气炸锅/跑鞋/保温杯）反推，避免凭空编造品牌。
- **验证**：「你们是做什么的？」→ RAG 命中，35ms 返回四大品类介绍 ✅

### 问题 2：RAG 数据库有漏洞 + 没利用好 LLM

- **现象**：RAG 很多情况没覆盖；Agent 查知识库时会把不相关内容塞给模型，导致答非所问。
- **根因**：`app/agent.py` 的 `search_faq` 工具调用 `rag.retrieve(top_k=3)` **不过滤距离**——向量检索「永远返回最接近的 3 条」，即使最接近的 3 条也毫不相关，照样回传给 LLM，污染上下文。
- **解决方法**：
  1. `search_faq` 增加 L2 距离过滤（复用 `RAG_MAX_DISTANCE` 阈值），只返回真正相关的条目，无关时明确返回「知识库暂无相关内容」，让 LLM 诚实兜底而不是拿垃圾硬答；
  2. 补充 FAQ 覆盖（产品介绍，与问题 1 同一批）。
- **验证**：「退货需要满足什么条件？」→ 命中准确答案；无关/情绪类消息 → 转人工兜底，不胡编 ✅

### 问题 3：转人工没有先安抚客户

- **现象**：升级人工时回复冷冰冰的「抱歉…已转接」，客户体验差。
- **根因**：`app/router.py` 三处转人工回复是硬编码模板，只有「抱歉+转接」，没有共情。
- **解决方法**：三处转人工回复统一改为「先共情安抚 → 再说明转接 → 再给预期」：
  - 低置信：「非常抱歉，我一时没能准确理解您的意思，让您久等了……」
  - 投诉/纠纷类：「非常抱歉给您带来不好的体验，我完全理解您的心情……」
- **验证**：「商家拒绝退款，多次沟通无果，我要投诉」→ 转人工，回复含「非常抱歉…完全理解您的心情」✅

### 问题 4：AI 回复速度慢

- **现象**：普通业务问题约 5.4 秒才出结果。
- **根因**：
  1. 每个非 RAG 请求要打 2 次 LLM（分类 + Agent）；
  2. `requests.post` 每次新建 TCP+TLS 连接（~数百 ms 握手）；
  3. `max_tokens` 偏大（分类 512、Agent 1024），生成冗余 token；
  4. **每次冷启动** embedding 模型都走 hf-mirror 逐文件 HEAD 校验（~90 秒）。
- **解决方法**：
  1. `llm.py` 用模块级 `requests.Session()` 复用连接（keep-alive）；
  2. Agent `max_tokens 1024→512`、`max_steps 4→3`；分类 `max_tokens 512→200`；
  3. `rag.py` 模型加载优先 `local_files_only=True`（本地缓存秒级加载），缓存缺失再回退联网下载。
- **验证**：RAG 命中 32~35ms；分类+转人工从 ~5.4s 降到 ~1s；重启冷启动从 ~90s 降到秒级 ✅

---

## 2026-08-15：「我的订单 / 查工单」跳人工 + 投诉被 RAG 吞掉

### 现象

1. 点「我的订单」「查工单」快捷按钮，AI 一律跳人工，而不是调工具查询；
2. 客户说「物流三天没更新，我要投诉」，AI 却回了句通用的「物流 6-12 小时更新一次」FAQ——投诉被知识库抢答。

### 根因

1. **跳人工**：`classifier.classify()` 对「帮我查一下我的所有订单」给了「物流查询 置信度 0.70」、对「查工单」给了「商品咨询 置信度 0.60」——低于 0.8 阈值，被升级人工。可这类「查自己的数据」根本不是投诉/纠纷，**根本不该交给分类器判断**。
2. **投诉被吞**：`router` 里 RAG 优先（`best_answer`）跑在分类器之前，投诉类文案只要语义上接近某条 FAQ，就被 RAG 命中并直接返回答案，绕过了转人工逻辑。

### 解决方法

`app/router.py` 新增两个意图识别，插在分类器之前：

1. **`_is_data_query()`「自助查询」**：命中「我的订单/查订单/我的工单/查工单/物流/快递/退款进度」等关键词（且不含投诉词）→ 直接交 Agent 调 `list_my_orders` / `check_my_tickets` / `query_logistics` / `check_refund`，跳过分类器。
2. **`_is_escalate_intent()`「升级人工意图」**：命中「投诉/赔偿/纠纷/扯皮/转人工/找人工」→ 置于 RAG 优先**之前**，直接升级人工（先安抚），绝不查 RAG、不自动作答。

处理流程最终变为 9 级（见 `router.py` docstring）：日常问答 → 缓存 → 闲聊 → 升级人工意图 → RAG → 多轮追问 → 自助查询 → 分类 → 兜底。

### 验证

- 「帮我查一下我的所有订单」→ Agent 列出 4 笔订单 ✅
- 「我提交的工单处理得怎么样了」→ Agent 汇总工单进度 ✅
- 「物流三天没更新，我要投诉」→ 71ms 直接升级人工（先安抚）✅

---

## 2026-08-15：「AI 客服好蠢」——一批实测定性 + 维修登记闭环

### 实测发现的「蠢点」与修复

| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| 1 | 「你好，耳机坏了」被当闲聊 | `is_smalltalk` 见「你好」就整条路由到闲聊 | 去掉问候词后仍有实质内容就不算闲聊 |
| 2 | 手里有订单数据却反问客户要订单号 | Agent 没查 `list_my_orders` | 提示词强制「能自己查就别麻烦客户」 |
| 3 | 「空气炸锅怎么用/优惠活动」答「没查到」 | `search_faq` 距离阈值 0.6 太严 | Agent 检索改用更宽松的 0.8（由 LLM 再判相关性） |
| 4 | 「我的耳机什么时候到」答「我一时没处理好」 | 之前为提速把 `max_steps` 4→3，多步查询被截断 | 恢复为 4 |
| 5 | 申请维修光说不练（不给流程、不登记） | 没有「登记维修工单」工具，维修流程不在知识库 | 新增 `apply_after_sale` 工具 + faq.md 补维修流程 |

### 对齐市面 AI 客服的「维修闭环」

申请维修现在走完整闭环（确认订单 → 讲流程 → 登记 → 给工单号）：

1. 第 1 轮：用 `list_my_orders` 查出对应订单，问具体故障；
2. 第 2 轮：讲清维修流程和时限（24h 响应 → 寄修免运费 → 3-7 工作日 → 保修期内免费）；
3. 第 3 轮：客户确认后调 `apply_after_sale` **真正登记工单**（记录订单号+故障，后台可查到、人工可跟进），返回工单号。

验证：工单 #158 已真实落库（`售后维修`，`[售后维修申请] 订单 A20240812001 问题：耳机无法开机`）。

---

## 2026-08-15：按 7 大模块规格落地（阶段一：情绪 + 多分类 + 留痕 + 降级）

> 用户给出 7 模块产品规格，要求「结合项目实际」实现（MySQL + DeepSeek，非 SQLite/Claude）。
> 分三阶段落地，本批为**阶段一**（模块 ①②③⑤ 核心）。

### 1. 数据层：全链路留痕（模块①）

- `tickets` 表新增 `phone / device / emotion / emotion_intensity / multi_intent / rag_chunks / route_trace` 字段（`_SCHEMA` 建新库 + `_migrate` 补老库，幂等）。
- 新增两张表：`ticket_logs`（状态流转/审计日志）、`ticket_feedback_tags`（人工负反馈标注，为模块⑦预埋）。
- 新增 `db.search_tickets()` 多维度检索（关键词/分类/状态/情绪/用户）；`db.insert_ticket_log()` / `log_status_change()` 记录状态流转。

### 2. 情绪识别 + 分层安抚（模块②）

- **关键设计**：情绪识别**折叠进分类器同一次 LLM 调用**，不额外多打一次大模型（避免拖慢回复，此前用户投诉过速度）。
- 分类器新增输出 `emotion`（负面/中性/正面）+ `emotion_intensity`（normal/extreme）。
- 分层安抚：`extreme` 极端负面 → 强制转人工（安抚话术）；`normal` 普通负面 → Agent 规则 7 先安抚再回答；中性/正面 → 正常流程。
- `metrics` 增加 `emotion_distribution` + `negative_ratio` 情绪统计。

### 3. 多诉求混杂 → 高危人工（模块③）

- 分类器新增 `multi_intent` 布尔输出；路由新增优先级 `极端负面 > 多诉求 > 低置信 > 纠纷类 > 自动`。
- 分类阈值 0.8 → 0.85，对齐产品规格（同时消除了 `.env.example` 注释写 0.85 但 config 默认 0.8 的不一致）。

### 4. 接口降级容错（模块⑤）

- 新增 `_run_agent_safe` / `_classify_safe` 包装 LLM 调用；DeepSeek 超时/报错/未配密钥时**不再 500**，统一降级转人工（安抚话术），保证客户问题不丢单。

### 验证（curl 实测）

- 「物流三天没更新，我要投诉」→ `emotion=负面/extreme`，转人工，留痕 `escalate_intent` ✅
- 「耳机收到就开不了机，太让人生气了」→ `售后维修 0.90 / 负面normal`，Agent 自动安抚处理 ✅
- 「我要退货，另外还想开发票」→ `multi_intent=1`，转人工，留痕 `escalate:multi_intent` ✅
- `/api/tickets/search?emotion=负面` → 命中 3 条；`?keyword=耳机` → 11 条 ✅
- 工单审计日志 `/api/tickets/{id}/logs` 正常返回 `created` 事件 ✅

### 排坑记录

- `/api/tickets/search` 最初定义在 `/api/tickets/{ticket_id}` **之后**，被动态路由遮蔽（`search` 被当 ticket_id 解析 int → 422）。修复：静态路由必须定义在动态路由之前。

---

## 2026-08-15：按 7 大模块规格落地（阶段二：RAG 知识库增强，模块④）

### 1. 知识源迁移到 MySQL（结构化 + 软删除 + 审计）

- FAQ 知识源从 `faq.md` 文件迁移到 MySQL `faq_entries` 表（`id / question / answer / enabled / created_at / updated_at`），`enabled` 是软删除标志。
- 新增 `rag.sync_faq_to_chroma()`：把「启用中」的条目同步到 ChromaDB（id 用 `faq-{主键}`），软删/编辑后立即生效，无需重启；同时清理向量库里已软删的旧条目。
- 新增 `update_entry / disable_entry / enable_entry`，实现知识库「编辑 + 软删除 + 恢复」。
- `scripts/seed_faq.py` 补 `db.init_db()`，避免首次建表缺失报错。

### 2. 召回 Top5 + LLM 重排 Top3

- `RAG_TOP_K` 3→5；`rag.rerank()` 用 LLM 对向量召回的 Top5 做相关性精排取 Top3，LLM 失败时降级为按距离排序。`agent.search_faq` 改为「召回 Top5 → 重排 Top3 → 距离过滤」。
- 文档切块 `200/40 → 500/50`，对齐规格。

### 3. 防幻觉生成约束

- `prompts/agent.py` 新增规则 14：回答政策/流程/时限/金额等事实性内容只能基于 `search_faq` 结果，知识库没有的具体数字/条件绝不允许编造。

### 验证（curl 实测，用一条临时测试条目走完整闭环）

- 新增 → RAG 立即命中（热更新生效）✅
- 编辑答案 → RAG 返回新答案 ✅
- 软删除 → RAG 不再命中，转人工兜底 ✅
- 恢复 → RAG 再次命中 ✅

### 排坑记录

- `scripts/seed_faq.py` 单独跑时报 `Table 'faq_entries' doesn't exist`：脚本没建表。修复：脚本里补 `db.init_db()`。

---

## 2026-08-15：按 7 大模块规格落地（阶段三：后台统计 + 负反馈闭环，模块⑥⑦）

### 1. 后台数据统计（模块⑥）

- 新增 `metrics.top_questions()`（TOP 高频问题）与 `metrics.emotion_trend()`（近 7 天逐日 负面/中性/正面 情绪趋势）。
- 新增 `/api/stats`（仅管理员）聚合返回 `top_questions / emotion_trend / feedback_tags`。
- 后台新增「数据统计」面板：TOP 问题榜单 + 情绪趋势堆叠条形图 + 负反馈标签分布，一眼看到知识库缺口和客户情绪变化。

### 2. 人工负反馈迭代优化闭环（模块⑦）

- 新增 `app/feedback.py`：`TAG_LABELS`（分类错误 / 知识库无答案 / AI回答有误 / 安抚不合适）四种人工标注；`suggest()` 按标签给出一条可落地的优化建议：
  - 知识库无答案 → 建议补 FAQ（优先复用人工已写答案，否则 LLM 起草 + 标记「需人工审核」）；
  - 分类错误 → 建议补分类器 few-shot 样例；
  - AI回答有误 → 建议回流/修正知识库条目；
  - 安抚不合适 → 建议优化安抚话术 / 补情绪识别样例。
- 新增 `POST /api/tickets/{id}/tag`：坐席打标 → 落库 `ticket_feedback_tags` + 写审计日志 + 返回优化建议。
- 后台升级工单卡片新增「标注 AI 错误」按钮组 + 卡片显示情绪标签。

### 3. 知识库后台管理补全（模块④ 收尾）

- 后台知识库列表新增「编辑 / 停用（软删）/ 恢复」按钮，配合「显示已停用」开关，形成完整 CRUD + 软删闭环。

### 验证（curl + 后台实测）

- `POST /api/tickets/166/tag {"tag":"知识库无答案"}` → 返回 `add_faq` 建议，LLM 起草答案 + `review_required=true` ✅
- `POST /api/tickets/167/tag {"tag":"分类错误"}` → 返回 `add_fewshot` 建议 ✅
- `GET /api/stats` → `top_questions` / `emotion_trend` / `feedback_tags` 全部有值 ✅

---

## 2026-08-15：补极端情绪防漏盲区——阴阳怪气/反话漏判

### 现象

复盘时发现防漏机制存在唯一「软」防线：不带投诉关键词（第 1 道关键词快路径漏）、模型也没判出 extreme 的隐性辱骂/阴阳怪气（如「你们可真专业啊」「呵呵」），可能被误判成「中性」而自动作答，客户被冷处理。

### 根因

`prompts/classify.py` 规则 6/7 的负面情绪定义太窄——负面只写了「不满/生气/着急/投诉/威胁/辱骂」，extreme 只写了「辱骂/强烈愤怒/威胁升级/索赔/扬言投诉媒体」，没覆盖中文里常见的「讽刺/反话/阴阳怪气」。这类话术字面不带脏字、表面中性，模型容易漏判。

### 解决方法

1. 规则 6 负面定义补「讽刺/阴阳怪气/反话/不耐烦」，并强调「表面中性实为强烈不满，必须判负面」。
2. 规则 7 extreme 定义补「持续阴阳怪气讽刺、扬言差评/曝光」。
3. 加一条讽刺类 few-shot 样例（样例9），对齐模型判断。
- 特意**不加关键词词库**：阴阳怪气用关键词根本兜不住，且「垃圾/真棒」这类词极易误伤正常咨询。

### 验证（curl 实测）

- 「呵呵，你们客服可真够专业的，等了三小时连个回音都没有」→ 负面/extreme，转人工 ✅
- 「这售后真棒呢，买个耳机坏了半天没人管」→ 负面/extreme，转人工 ✅
- 「请问退货需要什么条件？」→ 中性，RAG 自动回答（未误伤）✅

---

## 遗留事项（待处理，未在本次解决）

1. ~~**`.env.example` 存在不一致**：注释写「默认 0.85」，但 `config.py` 实际默认 `CONFIDENCE_THRESHOLD=0.8`。~~ ✅ 已在阶段一消除（config 默认统一为 0.85）。
2. **`.env.example` / `.gitignore` / `README.md`** 有上个会话遗留的未提交改动，需单独 review 后再决定是否提交。
3. ~~**P1 安全项**（越权修复 / bcrypt / 连接池 / 分层路由 / 测试 / Docker）~~ ❌ **已决定放弃**（2026-08-15 用户拍板）：项目已足够完整，按当前版本定稿。
4. **git 身份仍是占位**（`pc / pc@local`），推 GitHub 前需改成真实姓名/邮箱。

---

## 2026-09-10：用户表达「我生气了」却显示系统异常

### 现象

客户输入「我生气了」后，页面回复：

> 抱歉，系统开小差了，请稍后再试。

这会让客户感觉系统既没有理解情绪，也没有提供帮助。

### 根因

1. `app/router.py` 的服务反馈关键词没有覆盖「生气、愤怒、气死了、被气到了」等常见表达。
2. 消息未被识别为服务反馈，继续进入 RAG 检索。
3. 某些 RAG 命中结果带有数据库 `datetime` 字段，写入 `rag_chunks` 时直接调用 `json.dumps()`，触发：

   ```text
   TypeError: Object of type datetime is not JSON serializable
   ```

4. 后端因此返回 HTTP 500，`static/app.js` 的通用异常捕获又把所有错误统一显示成「系统开小差」。
5. 修复过程中还发现「气炸」会误命中「空气炸锅」，导致正常商品咨询被错误识别为负面情绪。

### 解决方法

1. 在路由层增加独立的愤怒情绪识别，覆盖：
   - 生气
   - 愤怒
   - 气死了
   - 被气到了 / 被气坏了
   - 烦死了 / 好烦
   - 火大 / 恼火
   - 气炸了 / 太气人
2. 纯情绪表达在 RAG 和分类器之前走本地即时安抚：
   - 不调用 LLM；
   - 不创建人工工单；
   - 返回情绪承接和业务引导。
3. RAG 记录序列化增加 `default=str`，避免时间字段导致正常请求 500。
4. 前端区分 API 业务错误和网络连接错误：
   - 优先展示后端可读的 `detail/reply`；
   - 非 JSON 服务器错误不暴露技术文本；
   - 只有真正连接失败才提示无法连接服务。
5. 更新 `app.js` 版本号，避免浏览器继续使用旧缓存。
6. 将「气炸」收紧为「气炸了 / 气炸我了」，避免误伤「空气炸锅」。

### 验证结果

- 「我生气了」→ HTTP 200，`reply_source=chat`，`status=auto`，返回安抚语 ✅
- 「我很生气」→ 不进入 RAG，不调用分类器，不转人工 ✅
- 「气死我了」→ 本地即时安抚 ✅
- 「空气炸锅怎么预约烹饪」→ 不再被识别为情绪反馈 ✅
- RAG 命中包含 `datetime` → 可以正常序列化并返回 ✅
- 全量测试：`172 passed` ✅
- Python 编译检查、JavaScript 语法检查、`git diff --check` 均通过 ✅

### 经验沉淀

- 情绪关键词必须做误命中测试，尤其要检查中文复合词，例如「气炸」和「空气炸锅」。
- 用户可感知的兜底提示不能掩盖后端业务错误，前端必须区分网络故障、API 错误和业务降级。
- RAG 命中结果来自数据库、缓存和向量库，落库前必须经过可序列化边界处理。

---

## 2026-09-10：阶段 2 首轮完成缓存与检索观测

### 目标

在调整切块大小、召回 Top-K 或 rerank 阈值之前，先建立可信的线上缓存和离线评测基线。

### 发现

- 项目原本已经有内存、Redis、语义缓存和向量检索，但统计字段混合了“缓存事件”和“请求次数”。
- 经理页面刷新时会触发离线评测，如果直接调用线上 RAG 统计，评测请求会污染缓存命中率。
- 首次向量命中没有明确来源标记，无法判断 FAQ 是由缓存命中还是向量检索命中。

### 解决方法

1. 为 FAQ 结果补充明确的 `cache_layer`：
   - `memory`
   - `redis`
   - `semantic`
   - `vector`
2. 增加按请求统计：
   - FAQ 查询数
   - 缓存命中数
   - 向量命中数
   - 直接回答数
   - 未回答数
   - LLM 节省数
3. `llm_saved` 改为按直接回答请求去重，避免一次请求被重复计算。
4. 离线评测使用独立统计上下文，不计入线上缓存数据。
5. 使用 `ContextVar` 隔离并发请求，避免评测暂停状态影响真实客户请求。
6. 经理仪表盘增加直接回答率和各缓存层命中数量。

### 验证

- RAG 与缓存专项测试：12 passed ✅
- `python -m compileall -q app` ✅
- `node --check static/manager.js` ✅
- `git diff --check` ✅

### 当前结论

阶段 2 已进入“有基线再调参”状态。下一轮先扩充评测集并记录当前基线，再决定切块大小和阈值是否需要修改。

---

## 2026-09-10：阶段 2 首轮检索质量优化

### 现象

离线评测中 FAQ 基本都能召回，但 Top3 候选包含多个相邻业务问题。早期指标表现为：

- Recall@5：100%
- Precision@3：33.33%

只看 Recall 会误以为检索已经完全可靠。

### 根因

1. 本地 CrossEncoder reranker 因模型文件缺失不可用。
2. fallback 仅按向量距离排序，「退货 / 退款」「发票下载 / 开发票」等中文近邻业务容易混排。
3. 评测集只允许单一标准问题，没有表达同一业务流程的多个可接受答案。
4. 只有 Precision@3，没有单独统计最终 Top1 答案是否正确。

### 解决方法

1. 增加 `Precision@1` 指标，单独衡量最终答案命中率。
2. CrossEncoder 不可用时，fallback 使用：
   - 中文二字短语重合；
   - 中文三字短语重合；
   - 业务词重合；
   - 向量距离作为次级排序。
3. 扩展发票、售后维修评测样本的可接受答案集合。
4. 保留 CrossEncoder 可用时的原有精排逻辑，不改变生产模型路径。

### 验证结果

- Recall@5：`100%`
- Precision@1：`100%`
- Precision@3：`42.86%`
- FAQ 命中率：`100%`
- 实时数据边界保护率：`100%`
- 全量测试：`175 passed` ✅
- Python/JavaScript 语法检查通过 ✅

### 当前判断

当前最终首答案质量已经达到目标，但候选集合纯度仍需提升。下一步重点不是继续堆关键词，而是扩大评测集、修复 CrossEncoder 模型供应，并评估中等粒度文档切块。

---

## 2026-09-10：真实口语评测与切块参数审计

### 现象

原有 8 条评测样本过于理想化，无法覆盖「签收后几天能退呀」「条见」「保不」「咋报修」等真实表达，也无法区分物流、退款进度和工单查询等实时数据问题。

### 解决方法

1. 将 RAG 评测集扩充到 23 条：
   - 16 条 FAQ；
   - 7 条实时数据/非 FAQ 边界问题。
2. 给边界样本增加 `guard_reason`，给 FAQ 失败样本增加 `failure_reason`、Top1 问题和距离。
3. 在问题归一化层补充口语和常见错别字：
   - 「能退呀」→ 退货；
   - 「条见」→ 条件；
   - 「保不」→ 保修；
   - 「咋报修」→ 申请维修；
   - 「地址填错了还能改么」→ 修改收货地址。
4. CrossEncoder 模型文件缺失时记录不可用状态，后续直接走 fallback，减少重复加载和告警。
5. 对 `420/60`、`280/40`、`360/50`、`500/80` 做切块对比。现有文档下 `420/60` 在上下文和粒度之间最平衡，因此保留默认值。
6. 将新增真实问法同步到 `data/faq_variants.json`，服务重启或知识库同步后可直接命中 FAQ 缓存。

### 验证

```text
专项测试：15 passed
评测样本：23
Recall@5：100%
Precision@1：100%
Precision@3：39.58%
边界保护率：100%
```

### 结论

「正常口语被转人工/答错」首先应修统一归一化和 FAQ 资产，不应直接把召回 Top-K 调得更大。当前 Top1 已达到评测目标，Top3 纯度仍需真实 CrossEncoder 或候选域约束继续优化。
## 2026-09-10：人工客服退货标注闭环

### 用户问题

人工客服只能看聊天内容。用户说“我要退货”时，客服无法明确记录商品、订单、原因和当前处理阶段。

### 根因

- 工单表负责会话和人工协作状态。
- 售后申请表负责真实的退货、退款、维修等业务申请。
- 两者之间没有客服工作台使用的结构化售后标注对象。
- 原有反馈标签只用于记录 AI 错误，不能复用为售后流程状态。

### 解决方案

- 新增当前标注表 `ticket_after_sale_annotations`。
- 每张工单只保留一条当前标注，修改历史写入 `ticket_logs`。
- 客服可记录：
  - 售后类型；
  - 处理阶段；
  - 订单号；
  - 商品；
  - 售后原因；
  - 商品状态；
  - 客服备注。
- 订单号填写后校验归属，防止客服误关联其他客户订单。
- 保存标注不创建真实售后申请，避免把“用户想退货”误当成“已经提交退货”。
- 标注保存后发布实时工单更新，其他客服页面可以刷新看到最新上下文。

### 页面操作

客服进入 `/staff` 后，在对应工单的“售后标注”区域选择“退货申请”，填写订单号和原因，点击“保存标注”。

示例：

```text
售后类型：退货申请
处理阶段：待核对订单
订单号：A20240812001
商品：无线蓝牙耳机
售后原因：商品不合适
商品状态：已签收
客服备注：客户明确要求退货，待核对退货规则
```

### 验证

- 新增售后标注测试通过。
- 订单归属校验测试通过。
- 标注不创建真实售后申请测试通过。
- 全量测试：`182 passed`。
- Python 编译、JavaScript 语法和 `git diff --check` 均通过。

## 2026-09-10：人工客服订单核验卡

### 用户场景

客服知道用户想退货，但只看到聊天内容，不知道具体商品是否已发货、是否签收、是否已经退款。

### 解决方案

- 客服在售后标注中输入订单号并保存。
- 系统按当前工单客户和订单号查询订单。
- 客服工作台显示只读“订单核验”卡：
  - 商品名称；
  - 订单状态；
  - 物流进度；
  - 运单号；
  - 退款记录；
  - 下一步处理提示。
- 订单核验信息来自订单表，不使用客服手动填写的商品状态覆盖真实订单状态。
- 订单号不属于当前客户时不返回订单信息。
- 这一步只读订单，不修改订单，不创建退货/退款申请。

### 示例

```text
售后标注：退货申请
订单号：A20240812001

订单核验
商品：无线蓝牙耳机
订单状态：已发货
物流：已到达长沙分拨中心，预计明天送达
处理提示：已发货，取消需转为物流拒收或签收后退货流程
```

### 验证

- 订单核验专项测试：`26 passed`。
- 前端 `staff.js` 语法检查通过。
- Python 编译检查通过。
- `git diff --check` 通过。

## 2026-09-10：订单号自动匹配商品修正

### 用户反馈

订单号应该自动匹配商品，不能让客服自己填写商品名称。

### 原因

上一版虽然能在保存后显示订单信息，但商品输入框仍然存在，且没有实时核验入口，客服体验不够直接，也存在前端误填商品的可能。

### 解决方案

- 增加订单核验接口：

```text
GET /api/escalations/{ticket_id}/order-context?order_id=订单号
```

- 客服输入订单号后失焦，系统自动核验。
- 也可以点击“核验订单”主动核验。
- 核验成功后自动显示订单真实商品和订单状态。
- 商品字段改成只读，客服不能修改。
- 保存标注时后端再次按客户归属查询订单，忽略前端传入的商品名。
- 订单号变更后自动清空旧商品，要求重新核验。

### 结果

```text
输入订单号
    ↓
按当前客户匹配订单
    ↓
自动带出真实商品
    ↓
客服补充退货原因和备注
    ↓
保存售后标注
```

订单不存在或不属于当前客户时，不会保存标注，也不会显示商品信息。

## 2026-09-10：客服提交真实退货申请

### 用户确认的任务

继续完善人工客服售后流程，让客服可以在核验订单和退货原因后真正提交退货申请，而不是只在工单上写一个“需要退货”的标记。

### 原问题

上一阶段已经有“售后标注”和“订单核验卡”，但存在一个企业系统不能接受的缺口：

- 客服可以手动选择“已提交售后”；
- 实际上没有生成 `after_sale_requests`；
- 客户看起来像提交成功，后台却没有真实申请号；
- 申请也无法关联来源工单，后续审计和追责不完整。

### 解决方式

1. 新增 `POST /api/escalations/{ticket_id}/return-request`，服务端重新读取工单标注和订单事实。
2. 退货规则按订单状态执行：待发货先取消，已发货/运输中先签收，已签收/已完成允许提交，已退货拒绝重复申请。
3. 使用稳定幂等键防止客服重复点击。已有草稿会推进为处理中，已处理请求直接返回原申请号。
4. 在 `after_sale_requests` 增加 `ticket_id` 并支持老库迁移，提交成功后写审计日志。
5. 禁止标注接口手动写入“已提交售后”，这个状态只能由真实退货申请接口产生。
6. 通过 `ticket_updated` 和 `after_sale_updated` 同步客服端与客户侧，展示申请号和处理中状态。

### TDD 验证

覆盖未确认、非退货标注、缺参数、跨客户订单、订单状态拦截、成功提交、重复点击、已有草稿复用、失败不更新假状态和数据库关联字段。

### 最终验证

```text
专项测试：24 passed
全量测试：201 passed
Python 编译：通过
JavaScript 语法检查：通过
git diff --check：通过
```

### 面试可讲的设计点

- 工单是协作对象，售后申请是业务对象，不能用一个状态字段代替两个领域对象。
- 任何会产生业务副作用的操作，都需要确认、权限、归属校验、状态校验、幂等和审计。
- 前端按钮只是入口，最终状态必须由后端事实和数据库写入决定。

## 2026-09-10：退款与维修客服真实提交

### 用户确认的任务

继续把客服工作台从“只聊天/只标注”升级为可以真正提交退款和维修申请。

### 审计结论

- 客户侧退款和维修 Skill 已经有确认、申请号和人工处理流程。
- 客服侧缺少真实副作用接口，客服只能修改售后标注。
- 不能让客服直接把阶段改成“已提交售后”，也不能复制退货逻辑后遗漏退款记录校验。

### 实现内容

- 新增退款接口：`POST /api/escalations/{ticket_id}/refund-request`。
- 新增维修接口：`POST /api/escalations/{ticket_id}/repair-request`。
- 新增 `AfterSaleRequestSubmit`，所有提交必须明确确认。
- 抽取统一提交内核，统一执行：
  - 工单和客服归属校验；
  - 标注类型校验；
  - 订单归属重新校验；
  - 订单状态拦截；
  - 原因必填；
  - 幂等键复用；
  - 申请号和 `ticket_id` 关联；
  - `processing` 状态；
  - “已提交售后”标注；
  - `after_sale_submitted` 审计；
  - 客服端和客户侧实时通知。
- 退款专门拦截订单已有退款记录。
- 客服前端根据类型动态显示正确按钮和确认文案。

### 验证

```text
退款、维修、退货专项测试：28 passed
```

### 下一步

取消订单是下一条流程。它不仅创建售后申请，还会直接把订单从“待发货”改为“已取消”，因此单独实现事务和失败回滚，不能简单套用退款/维修申请流程。

## 2026-09-10：客服取消订单真实执行闭环

### 用户要解决的问题

客服工作台已经可以标注“取消订单”，但如果只是保存标注，订单本身仍然是“待发货”，客户和后台会看到互相矛盾的结果。企业系统必须让订单状态、售后申请和工单标注保持一致。

### 这次怎么解决

1. 新增 `POST /api/escalations/{ticket_id}/cancel-request`。
2. 服务端重新读取当前工单和客户订单，不信任前端传来的商品名、客户名或订单状态。
3. 只有“待发货”订单允许直接取消；已发货或运输中明确引导快递拦截、拒收或签收后退货退款。
4. 使用 `SELECT ... FOR UPDATE` 锁定订单，在同一个事务中：
   - 把订单更新为“已取消”；
   - 创建或复用取消申请；
   - 把申请写为“已完成”。
5. 事务任一步失败都会回滚，避免出现“订单已取消但没有申请”或“申请已完成但订单没取消”。
6. 成功后才把客服工单标注推进为“已完成”，并通过 `ticket_updated`、`after_sale_updated` 刷新客服和客户页面。
7. 前端根据售后类型显示“提交取消订单”，完成后显示“取消订单已完成”，不会再把完成状态显示成处理中。

### 验证结果

```text
取消订单专项测试：33 passed
全量回归：210 passed
Python compileall：通过
JavaScript 语法检查：通过
git diff --check：通过
```

### 面试讲法

这个功能的重点不是加一个按钮，而是把客服动作接入真实订单领域：工单负责协作上下文，订单负责状态事实，售后申请负责业务留痕。通过订单行锁、事务和幂等键，保证重复点击和并发请求不会制造重复申请或不一致状态。

## 2026-09-10：售后申请筛选与审计查询

### 用户要解决的问题

之前客服和经理只能从工单卡片里看到申请号，无法快速按“退款/退货/取消订单”和“处理中/已完成”筛选，也看不出是哪位客服、在什么时候完成了哪一步。

### 这次怎么解决

1. 复用 `after_sale_requests` 保存申请当前状态，复用 `ticket_logs` 保存来源工单操作历史，不新增重复审计表。
2. 客服新增售后队列查询：
   - 按售后类型筛选；
   - 按申请状态筛选；
   - 按订单号筛选。
3. 经理新增全量售后申请追踪区和单申请审计弹窗。
4. 客服可以从工单申请卡打开处理记录时间线。
5. 后端统一做参数白名单校验，避免前端传入任意状态。
6. 查询结果只返回申请号、订单号、业务类型、状态、工单摘要和处理人，不返回客户用户名或数据库内部 id。
7. 内部日志动作转换成业务语言，例如“提交售后申请”“完成取消订单”，客服不需要理解数据库动作名。

### 接口

```text
GET /api/after-sales/queue
GET /api/escalations/{ticket_id}/after-sale-audit
GET /api/manager/after-sales
GET /api/manager/after-sales/{request_no}/audit
```

### 验证结果

```text
售后专项测试：44 passed
全量回归：217 passed
Python compileall：通过
JavaScript 语法检查：通过
git diff --check：通过
```

### 面试讲法

查询和业务写入分离：写入接口负责状态一致性，查询接口负责可读性和权限边界；审计不重新推断状态，而是从业务申请和工单日志两个事实源聚合展示。

## 2026-09-10：阶段 4.8 售后失败尝试与重试

### 用户确认的任务

继续增加售后失败记录和可重试入口，保证失败原因、操作人和尝试次数可追踪，并且重试不产生重复申请。

### 设计约束

- `after_sale_requests` 保存申请当前状态。
- `after_sale_attempts` 保存每次真实执行尝试。
- 重试复用原申请号和原幂等键。
- 重试前重新校验工单、客服权限、客户归属、订单状态和售后类型。
- 取消订单重试不能再次修改已取消订单。

### 当前进度

- 已批准设计，开始按测试驱动方式实现。

### 实现结果

1. 在数据库中新增 `after_sale_attempts`，每个申请可以拥有多次执行尝试。
2. 每次尝试都记录申请号、尝试次数、状态、操作人、时间、错误码和错误原因。
3. 客户侧退款、维修、退货确认和取消执行接入尝试记录。
4. 客服侧退货、退款、维修、取消订单接入统一失败记录。
5. 新增客服重试接口：

```text
POST /api/escalations/{ticket_id}/after-sale-retry
请求体：{"request_no": "原申请号", "confirmed": true}
```

6. 重试前重新验证工单权限、客户归属、售后类型和订单当前状态。
7. 重试不会新建申请号，也不会重复取消订单。
8. 客服工作台显示失败原因和“重新提交”；经理和客服审计显示尝试时间线。
9. 客户侧隐藏客服账号，只看到可理解的失败状态和业务原因。

### 验证结果

```text
售后专项测试：51 passed
全量测试：224 passed
Python compileall：通过
JavaScript node --check：通过
git diff --check：通过
```

### 面试讲法

售后申请的当前状态和执行历史是两个不同维度：当前状态用于业务查询，尝试表用于可靠性和审计。重试使用原申请号和原幂等键，并在重试前重新核验订单事实，避免“失败后重复生成申请”以及“订单状态已经变化仍然强行执行”的问题。

## 2026-09-11：建立项目任务总结机制

### 用户要解决的问题

项目已经完成了多轮功能迭代，但任务结果分散在聊天记录、进度日志和解决日记里。继续开发时不容易快速知道“做过什么、为什么这样做、还缺什么”，面试时也不方便按任务讲清楚。

### 这次怎么解决

1. 新增 `任务总结.md` 作为项目任务总账，按日期和任务记录每轮迭代的目标、结果、验证和遗留问题。
2. 新增 `任务总结模板.md`，固定新任务的总结字段，避免只写“完成了某功能”而漏掉根因和验证。
3. 明确现有五类文档的分工：
   - `task_plan.md` 管未来计划；
   - `progress.md` 管执行过程；
   - `findings.md` 管技术结论；
   - `解决日记.md` 管问题排查；
   - `任务总结.md` 管任务级回顾。
4. 在 README 增加文档入口，GitHub 访问者可以直接看到项目如何持续演进。

### 后续执行规则

- 每个阶段完成后，先更新任务总账，再补进度和解决日记。
- 每个总结必须包含至少一条验证结果和一条遗留问题。
- 新的技术决策、安全边界或数据事实必须进入 `findings.md`。

### 面试讲法

我没有把项目文档全部堆在一个 changelog 里，而是按“计划、执行、发现、问题复盘、任务总账”分层。这样继续开发时可以快速恢复上下文，出现线上问题时能追溯根因，面试时也能用任务总账讲清楚每次迭代带来的业务价值和工程保证。
