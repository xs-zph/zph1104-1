# 项目发现与技术结论

> 本文件记录当前 AI 客服项目的事实、根因和设计决策，不记录简历等无关任务。

## 项目定位

- 项目是一个企业级电商 AI 客服工单系统，客服名称为“小鹏”。
- 核心链路是：客户聊天 → 路由判断 → FAQ/RAG、业务 Skill、MCP 工具或人工客服 → 工单和审计落库。
- 主要角色：客户、客服、经理、系统管理员。
- 主要后端入口：`app/main.py`、`app/router.py`。
- 主要检索模块：`app/rag.py`。
- 主要前端入口：`static/app.js`、`templates/index.html`。

## 已确认的核心路由顺序

1. 隐私脱敏
2. 纯服务反馈和情绪安抚
3. 订单/物流/退款/工单等实时查询
4. 订单操作和售后 Skill
5. 闲聊与助手元对话
6. 明确投诉、纠纷、赔偿或转人工
7. 退款澄清
8. RAG FAQ 命中
9. 多轮追问
10. 分类器与 Agent
11. 业务化降级或人工兜底

## 近期问题：情绪表达导致 500

### 根因

- “我生气了”没有被服务反馈关键词识别。
- 消息进入 RAG 后，命中结果中的 `datetime` 无法被 `json.dumps()` 序列化。
- HTTP 500 被前端通用 catch 统一改写为“系统开小差”。

### 修复

- 增加愤怒/烦躁表达识别，提前返回本地安抚。
- RAG 记录序列化增加字符串兜底。
- 前端解析后端 `detail/reply`，区分 API 错误和网络错误。
- 关键词增加误命中回归，避免“空气炸锅”被“气炸”误判。

### 验证

- 全量测试：172 passed。
- 真实 HTTP：`我生气了` 返回 200、`reply_source=chat`、`status=auto`。
- `空气炸锅怎么预约烹饪` 不再命中情绪反馈。

## 当前架构事实

### 缓存

- FAQ 已有进程内缓存、Redis 缓存和语义缓存的基础实现。
- `app/rag.py` 已记录缓存命中层和统计数据。
- 后续重点是统一缓存失效、评测指标和经理端展示，而不是重新发明缓存结构。

### RAG

- FAQ 使用向量检索，支持 rerank 降级。
- FAQ 和文档检索存在不同的业务阈值：FAQ 直接回答要更严格，Agent 辅助检索可以更宽松。
- 需要持续防止个人订单、物流和退款进度误命中静态 FAQ。
- 文档切块必须在“上下文完整”和“检索粒度”之间取平衡，不能只追求块越大或越小。

### Skills/MCP

- 订单、物流、退款查询已有 MCP 只读接入，并有内置工具回退。
- 退款、退货、维修、取消订单已开始使用场景化 Skill。
- 有副作用的操作必须继续遵守：先确认、再执行、幂等、权限校验、审计。

### 前端

- 客户端存在 API 错误、服务降级、人工处理中等多种回复状态。
- 静态脚本通过 query version 更新缓存。
- 客户能看到的文案必须使用“订单查询、处理中、已完成”等业务语言，不能直接暴露 `escalated`、`human_queue` 等内部状态。

## 当前风险

1. 关键词路由容易出现中文子串误命中，新增词必须同时增加反例测试。
2. RAG 结果来自数据库、缓存和向量库，落库和 API 返回前都要经过可序列化边界。
3. “转人工”不能成为所有异常的默认出口；上游失败应优先返回可重试的业务化降级。
4. 指标如果没有统一事件定义，经理端的召回率、精确率和自动处理率会失真。
5. 当前工作区有较多未提交历史改动，提交 GitHub 前必须按文件审查，不能直接整体提交。

## 2026-09-10 阶段 4.8 设计确认

- `after_sale_requests.status = failed` 只能表达当前结果，不能表达多次执行尝试。
- 失败原因目前保存在 `result`，再次更新申请状态时容易被覆盖。
- 重试必须以 `request_no + idempotency_key` 为原申请身份，不允许重新调用创建申请逻辑生成新号。
- 取消订单需要额外防止重试再次修改已经是“已取消”的订单。
- 客服重试前仍需重新读取工单、客服归属、客户订单和售后标注，不能只根据前端申请号放行。

## 2026-09-10 阶段 4.8 实现结果

- 新增 `after_sale_attempts`，申请当前状态和执行历史分离。
- 每次真实售后提交先记录 `started`，成功或异常后更新为 `succeeded` / `failed`。
- 失败申请的 `result` 可以继续被业务状态更新使用，但详细失败原因仍保存在尝试表。
- 重试接口使用原 `request_no`，并核对客户、工单、售后类型、订单号和幂等键对应关系。
- 取消订单重试复用原子取消事务；订单已取消时按幂等完成，不再次执行订单状态变更。
- 经理和客服查询只显示业务字段；客户查询不显示坐席账号。
- 最近一次尝试汇总改为“总数查询 + 最新记录查询”，避免成功尝试因空错误字段显示旧失败原因。

### 阶段 4.8 验证

- 售后专项测试：`51 passed`
- 全量测试：`224 passed`
- 既有弃用警告仍存在，但没有测试失败。

## 下一阶段技术决策

- 先做评测集和指标口径，再调整缓存、切块和 rerank 参数。
- 先保证离线评测结果可复现，再把结果接入经理仪表盘。
- 先完善副作用 Skill 的协议和测试，再扩展更多 MCP 写操作。

## 2026-09-10 阶段 2 初始审计

- `app/rag.py` 已经具备内存 LRU、Redis、语义缓存和向量检索四层能力。
- 当前缓存统计中的 `miss` 只记录精确缓存未命中，语义缓存命中发生在其后，统计解释容易混淆。
- `faq_direct`、`memory_hit`、`redis_hit`、`semantic_hit` 目前都被算入 LLM 节省，但“同一次请求”的去重和总请求数还需要统一。
- `app/rag_eval.py` 已支持 Recall@K、Precision@K、FAQ 命中率和实时数据保护率，下一步应扩展缓存层、检索层和拒答原因字段。
- 经理页面已经显示 Recall@5、Precision@3、FAQ 命中率、边界保护率和缓存命中率，但还没有展示向量命中、语义命中和缓存节省比例的分解。

### 本轮完成

- `app/rag.py` 新增按请求统计：
  - `query_count`
  - `cache_hit_count`
  - `vector_hit_count`
  - `answer_hit_count`
  - `no_answer_count`
  - `layer_hits.memory/redis/semantic/vector`
- `llm_saved` 改为按“可直接返回 FAQ 的请求数”统计，避免缓存写入和缓存读取重复累加。
- `app/rag_eval.py` 在 `suspend_cache_stats()` 上下文中运行，离线评测不会污染线上缓存数据。
- 使用 `ContextVar` 隔离评测暂停状态，避免并发客户请求被错误暂停统计。
- 经理端增加直接回答率和缓存层分解。

### 当前基线原则

- 先记录指标，再改检索参数。
- 评测集请求不计入线上缓存命中率。
- “Recall 高”不代表“可以直接回答”，直接回答还要同时看 Precision、距离阈值和边界保护率。

### 本轮评测基线

- 评测样本：8 条，其中 FAQ 样本 7 条、实时数据保护样本 1 条。
- Recall@5：`100%`
- Precision@1：`100%`
- Precision@3：`42.86%`
- FAQ 命中率：`100%`
- 实时数据边界保护率：`100%`
- CrossEncoder 当前因本地模型文件缺失不可用，使用中文词面 + 向量距离 fallback。
- Precision@3 偏低表示候选集合仍混入相邻 FAQ，但最终 Top1 已全部命中；后续应扩大评测集后再调阈值。

## 2026-09-10 评测集扩充与切块审计

- 评测集从 8 条扩充到 23 条：
  - FAQ 样本 16 条；
  - 实时数据/非 FAQ 边界样本 7 条；
  - 增加口语、错别字、退款/退货/维修/发票相邻场景。
- 评测明细新增 FAQ Top1 问题、距离、失败原因，以及边界样本的原因和失败原因汇总。
- 通过统一 `normalize_question()` 增加中文口语和常见错别字归一化：
  - 「能退呀」「条见」「保不」「咋报修」「地址填错了还能改么」均能映射到对应业务表达。
- `data/faq_variants.json` 已同步新增口语问法，重启或知识库同步后会进入精确缓存和语义缓存。
- CrossEncoder 本地模型文件缺失时记忆不可用状态，后续请求直接走 fallback，避免每个 FAQ 样本重复加载模型。
- 切块审计结果：
  - 当前默认 `RAG_CHUNK_SIZE=420`、`RAG_CHUNK_OVERLAP=60`；
  - 两份现有文档分别切成 2 块，组合测试切成 4 块；
  - `280/40` 会切成 6 块，`500/80` 会切成 3 块；
  - 420/60 在上下文完整性和检索粒度之间更平衡，本轮不改参数。
- 新评测基线：
  - 样本 23；
  - FAQ 16；
  - 实时/边界 7；
  - Recall@5 `100%`；
  - Precision@1 `100%`；
  - Precision@3 `39.58%`；
- 边界保护率 `100%`。

## 2026-09-10 人工客服售后标注审计

### 当前事实

- `tickets` 已承载客户会话、人工接管、客服接单、回复、转派和结束状态。
- `after_sale_requests` 已承载真正的取消订单、退货、退款和维修申请，拥有订单号、原因、处理状态、结果和幂等键。
- `ticket_feedback_tags` 目前用于错误反馈标签和知识库优化，不应复用为退货业务标签。
- 客服端已有 `/api/escalations` 以及接单、转派、回复、状态推进、结束工单接口。
- 当前客服卡片只展示工单分类和内部协作状态，缺少“退货意向/退货待补充信息/售后处理状态”等结构化业务信息。

### 领域边界

- “用户想退货”是意图识别结果，不代表已经提交退货申请。
- “客服标注需要退货”是会话上的业务分类，允许先标注、后补信息。
- “提交退货申请”会产生业务副作用，必须继续经过订单校验、确认、幂等和审计。
- 已发货、已签收、商品质量问题等信息会影响退货处理路径，不能由一个“需要退货”标签直接承诺结果。

### 设计方向

- 在人工工单上增加结构化售后标注记录，至少区分售后类型、处理状态、订单号、商品/商品行、原因、商品状态、客服备注和最后操作人。
- 标注动作与提交售后申请分开；标注只更新客服工作台上下文，提交申请仍走既有售后流程。
- 每次标注和状态变化写入工单日志，并通过现有人工工作台事件刷新，避免客服只看到聊天文本。
- 客户可见信息继续使用“退货申请/待补充订单号/处理中”等业务语言，不显示内部字段名。

## 2026-09-10 人工客服订单核验卡

### 当前事实

- 仅有售后标注时，客服仍需手动填写商品和商品状态，无法确认订单系统中的真实状态。
- 订单表已经有商品、订单状态、运单号、物流和退款状态字段。
- 工单有客户用户名，订单查询可以按“客户 + 订单号”做归属校验。

### 领域边界

- `order_context` 是订单系统的只读事实。
- `after_sale_annotation.item_status` 是客服记录的处理观察，不能覆盖订单真实状态。
- 订单核验不等于取消订单、退款或退货申请，不产生业务副作用。

### 本轮完成

- 工单列表根据售后标注订单号加载当前客户的订单。
- 对外只返回订单号、商品、订单状态、物流、运单号和退款状态。
- 根据订单状态提供客服提示：
  - 未发货：继续核对取消或售后规则；
  - 已发货/运输中：进入拒收或签收后退货流程；
  - 已签收/已完成：核对退货时效和商品状态；
  - 已退货/已取消：避免重复办理；
  - 有退款记录：避免重复退款。
- 跨客户订单不返回核验信息。

## 2026-09-10 订单号匹配商品修正

### 问题

- 上一版需要保存标注后才能看到商品。
- 标注表仍保留可手填商品字段，存在商品与订单不一致的风险。

### 决策

- 订单号是商品匹配的唯一入口。
- 客服前端商品字段只读。
- 后端保存标注时必须按“工单客户 + 订单号”重新查询订单，并使用订单表商品覆盖前端商品值。
- 订单号改变后，前端清空旧商品并要求重新核验。

### 接口

- `GET /api/escalations/{ticket_id}/order-context?order_id=...`
- 返回业务化的订单核验信息，不暴露客户用户名和数据库内部 id。

## 2026-09-10 客服真实退货申请提交

### 发现

- 之前人工客服可以把标注阶段手动改成“已提交售后”，但不会创建 `after_sale_requests`，会造成前台显示成功、后台没有真实申请记录的假状态。
- 客服工作台只能保存售后标注，没有真实的副作用提交按钮。
- `after_sale_requests` 没有来源工单字段，无法从售后申请反查客服处理上下文。

### 处理原则

- 将“标注”和“真实申请”作为两个明确领域对象。
- 前端只负责收集确认，后端重新读取工单标注和订单事实。
- 订单状态不满足退货条件时，返回客服可执行的下一步，不创建申请。
- 申请创建采用幂等键，并在申请表中保存 `ticket_id`。
- 只有申请创建或复用并成功推进到 `processing` 后，才更新标注阶段。
- 通过审计日志和事件流把结果同步给客服端和客户侧。

### 当前接口

```text
POST /api/escalations/{ticket_id}/return-request
请求体：{"confirmed": true}
```

### 状态流

```text
客服标注退货
    ↓
核验客户订单和真实商品
    ↓
填写退货原因并确认
    ↓
创建/复用 after_sale_requests
    ↓
推进为 processing
    ↓
更新标注为 submitted
    ↓
写审计日志 + 通知客服和客户
```

### 测试边界

- 未确认、非退货标注、无订单号、无原因：拒绝。
- 跨客户订单：拒绝。
- 待发货、运输中、已退货：拒绝且不创建申请。
- 已签收、已完成：创建或复用申请。
- 重复点击：返回原申请号。
- 数据库创建失败：不更新“已提交售后”标注。

## 2026-09-10 退款与维修客服提交

### 现状

- 客户侧退款和维修已有确认型 Skill，可创建统一售后申请。
- 客服侧此前只有退货真实提交接口，退款和维修只能聊天或手动标注。

### 统一协议

- 退款与维修使用同一后端提交内核，只由业务类型、成功文案和订单规则配置差异。
- 接口不接收前端商品和客户字段，全部从工单标注和客户订单重新读取。
- 退款订单已有 `refund_status` 时拒绝重复申请。
- 维修允许已发货、运输中、已签收、已完成的订单；已取消订单拒绝。
- 两类申请均写 `ticket_id`，并在成功后发布 `ticket_updated`、`after_sale_updated`。

### 接口

```text
POST /api/escalations/{ticket_id}/refund-request
POST /api/escalations/{ticket_id}/repair-request
请求体：{"confirmed": true}
```

### 前端行为

- 退货标注显示提交退货申请。
- 退款标注显示提交退款申请。
- 维修标注显示提交维修申请。
- 已有退款记录或订单状态不满足规则时不显示按钮，后端仍会再次拒绝绕过前端的请求。

## 2026-09-10 客服取消订单闭环

### 关键发现

- 客户侧已有 `cancel_order` 工具，但它只适合客户自助场景，不能替代客服工单接口，因为客服场景还必须关联 `ticket_id`、写审计和刷新工作台。
- 取消订单是订单状态写操作，不是普通售后申请。只创建 `after_sale_requests` 而不更新 `orders` 会造成“申请完成但订单仍待发货”的数据矛盾。
- 订单状态和取消申请必须在同一数据库事务内完成，并锁定订单行，才能防止双击或并发客服造成两个结果。

### 已落地规则

- 只有“待发货”允许在线取消。
- 已发货、运输中、派送中返回快递拦截/拒收/退货退款指引，不生成取消申请。
- 已取消订单按幂等成功处理，返回原取消申请。
- 真实取消成功后，售后申请状态为 `completed`，客服工单标注阶段为 `completed`。
- “已提交售后”和“已完成”均不能由普通标注接口伪造；前者由真实售后提交接口更新，后者由取消订单真实执行接口更新。

### 风险与后续

- 取消订单事务目前依赖 MySQL/InnoDB 行锁，生产环境需要继续验证多实例连接池和死锁重试策略。
- 当前取消成功后的订单通知沿用 `ticket_updated` 和 `after_sale_updated`，后续可以增加订单专用事件供订单详情页实时刷新。

## 2026-09-10 售后申请查询与审计

### 关键发现

- `after_sale_requests` 是业务申请当前状态的事实来源，`ticket_logs` 是客服处理过程的审计来源；两者应该查询时聚合，而不是再维护一张重复审计表。
- 客服工作台需要按“售后类型/申请状态”筛选工单，但真正的申请状态只在真实售后申请创建后存在，不能把售后标注阶段当成申请状态。
- 审计详情可以按 `ticket_id` 关联回工单，但对客服和经理展示时应隐藏客户用户名和数据库内部 id，只保留申请号、订单号、工单号、处理人和业务动作。

### 权限结论

- 客户：只查自己的 `/api/after-sales`。
- 客服：需要 `ticket.view`，查售后队列和自己工作台范围内的来源工单审计。
- 经理：查全量售后申请和来源工单审计，接口只读。

### 后续风险

- 当前审计查询是实时扫描申请与工单日志；数据量大后需要增加按 `ticket_id`、`status`、`request_type` 和时间的索引策略。
- 当前客服审计弹窗展示的是来源工单日志，下一步可增加专门的售后状态变更事件，区分“标注变化”和“申请状态变化”。

## 2026-09-11 任务总结文档体系

- 项目已经进入多阶段持续迭代，仅靠 `解决日记.md` 会混合问题复盘、技术结论和未来计划。
- 采用职责分离：
  - `task_plan.md` 面向未来；
  - `progress.md` 面向执行过程；
  - `findings.md` 面向技术知识；
  - `解决日记.md` 面向问题解决；
  - `任务总结.md` 面向任务级快速回顾。
- 任务总结不替代详细文档，只保存每次任务的用户问题、结果、验证和下一步，避免重复粘贴大段实现细节。
- 每次阶段完成后至少同步任务总账、进度日志和解决日记；如果出现新的架构或安全结论，再同步发现记录。
